/* ============================================================
   dummies-extra.js — "For Dummies" plain-English intros for the
   newer topics. Extends CONTENT_DUMMIES (defined in content.js).
   ============================================================ */
Object.assign(CONTENT_DUMMIES, {
  'shannon': String.raw`<p>Imagine a <b>highway</b> between two cities. No matter how good your car is, there's a hard limit to how many cars per hour the road can carry — set by how many lanes it has and how clear the road is. Shannon's famous result is exactly that speed limit, but for information: it tells you the absolute fastest you can send data error-free over a given channel.</p><p>Two things set the limit: how <b>wide</b> the channel is (its bandwidth — the number of lanes) and how <b>clean</b> it is (the signal-to-noise ratio — how foggy the road is). Widen the road or clear the fog and you can push more data through; but past a point, adding more "engine power" (transmit power) barely helps — you've hit the wall.</p><p>This single idea underpins all of communications. Every modem, every Wi-Fi chip, every 5G tower is really an attempt to sprint as close to Shannon's speed limit as physics and cleverness allow — and none can ever beat it.</p>`,
  'source-coding': String.raw`<p>Source coding is <b>smart packing</b>. Think of texting "see you later" as "cu l8r" — same meaning, far fewer characters. Compression finds the wasteful, predictable parts of your data and writes them down more briefly, so the message takes up less space without losing what matters.</p><p>The trick is that common things get <b>short codes</b> and rare things get longer ones — exactly like Morse code gives the common letter "E" a single dot but rare "Q" a long dash-dash-dot-dash. There's a hard floor to how far you can squeeze (called the entropy), and clever schemes like ZIP, JPEG and MP3 get remarkably close to it.</p><p>This is why a photo shrinks from megabytes to kilobytes, and why your music streams without eating your whole data plan. It's the "remove the fluff" step — the mirror image of channel coding, which deliberately adds carefully-chosen fluff back to survive a noisy channel.</p>`,
  'sinc-function': String.raw`<p>Drop a stone in a pond and you get a big central splash with <b>ripples</b> that fade out on either side. The sinc function is math's version of that shape: a tall peak in the middle with smaller and smaller wiggles spreading out. It shows up everywhere you slice a signal cleanly in frequency.</p><p>Its special magic is that it's exactly <b>zero at regular spacings</b> — so if you line up one sinc's peak with its neighbours' zeros, they don't step on each other's toes. That's the secret behind sending symbols back-to-back without them smearing together, and behind perfectly rebuilding a signal from its samples.</p><p>There's also a neat see-saw: a wide, gentle sinc in time means a narrow, sharp shape in frequency, and vice-versa. That "you can't be narrow in both at once" trade-off is one of the deepest ideas in signal processing.</p>`,
  'frequency-spectrum': String.raw`<p>A spectrum is a signal's <b>ingredient list</b>. Just like a smoothie is really "two bananas, a cup of milk, a spoon of honey," any signal is really "a strong 100 Hz tone, a medium 250 Hz tone, a faint 1 kHz whistle." The spectrum simply lists which pure tones are inside and how loud each one is.</p><p>Instead of watching a confusing wiggle bounce up and down over time, you flip your view and look at it by <b>frequency</b> — and suddenly the hidden structure pops out as a few clean bars or spikes. A pure whistle is one spike; a rich chord is several; static is a smear across everything.</p><p>This is what a spectrum analyzer on a lab bench (or the little dancing bars in a music app) shows you. Engineers live in this view because it instantly reveals what's a signal, what's interference, and whether anything is spilling where it shouldn't.</p>`,
  'fft': String.raw`<p>The FFT is a <b>ridiculously fast machine for reading a signal's ingredient list</b> (its spectrum). Working out the recipe the slow, obvious way takes ages for long signals; the FFT uses a clever divide-and-conquer shortcut to do the same job hundreds or thousands of times faster.</p><p>Picture sorting a huge deck of cards: instead of comparing every card to every other, you split the deck in half, sort each half, then merge — vastly less work. The FFT does the frequency version of that split-and-merge trick, turning a job that would take forever into one that finishes in the blink of an eye.</p><p>Because it's so fast, the FFT quietly runs inside almost everything: your phone finding Wi-Fi channels, noise-cancelling headphones, medical scanners, music tuners, and every radio that works in the frequency domain. It's arguably the most-used algorithm in modern technology.</p>`,
  'fir-filters': String.raw`<p>A filter's job is to let some frequencies through and block others — like a bouncer deciding who gets into a club. An FIR filter does this by <b>blending a handful of the most recent samples</b> together with fixed weights. It has no memory beyond that short window, so it's simple, predictable, and can never misbehave.</p><p>Think of a "moving average" that smooths a bumpy graph by averaging each point with its neighbours — that's the simplest FIR. Use more neighbours (more "taps") and you get a sharper, choosier filter, at the cost of more arithmetic and a bit more delay.</p><p>Its big selling points: it's <b>always stable</b> (it can't blow up) and it delays every frequency by the same amount (linear phase), which keeps waveforms undistorted. That reliability is why FIR filters are the go-to when you need clean, trustworthy behaviour.</p>`,
  'iir-filters': String.raw`<p>An IIR filter is a filter with <b>feedback</b> — it blends in not just recent inputs but also its own recent outputs, like a room with an <b>echo</b> that keeps ringing after the sound stops. That echo lets a tiny amount of hardware produce a very sharp, powerful filtering effect.</p><p>Compare it to a savings account earning interest: a single rule, applied over and over to its own result, builds up a big effect cheaply. An IIR filter gets the same steep cutoff as a big FIR filter but with a fraction of the computation — great when you're short on processing power.</p><p>The catch is that feedback is a double-edged sword: pushed too far it can <b>ring forever or blow up</b> (go unstable), and it delays different frequencies by different amounts. So IIR filters trade the FIR's rock-solid safety for efficiency and punch — a classic engineering bargain.</p>`,
  'convolutional-codes': String.raw`<p>Convolutional coding protects data by <b>weaving each bit into its neighbours</b>, like overlapping stitches in a seam so that if one thread snaps the whole thing doesn't unravel. Instead of protecting bits one at a time, it spreads each bit's influence across several output bits, so the message has built-in overlap and context.</p><p>The encoder is just a little <b>sliding memory</b>: bits shuffle through a short register, and at each step it spits out a couple of coded bits that depend on the last few inputs. Because everything overlaps, a receiver that gets a garbled patch can look at the surrounding context and reconstruct what must have been sent.</p><p>Paired with the Viterbi decoder (which finds the most likely original path), convolutional codes powered generations of mobile phones, Wi-Fi, satellite and deep-space links. They're the classic "smear the data together so no single hit is fatal" approach to error correction.</p>`,
  'channel-coding': String.raw`<p>Channel coding is <b>deliberately adding smart backup</b> so a message survives a noisy trip. It's like spelling a name over a bad phone line as "<b>B for Bravo, A for Alpha</b>" — those extra words are redundant, but they let the listener recover the right letters even if some sounds get lost.</p><p>The receiver isn't just handed the raw bits; it's handed bits plus carefully-chosen extra "check" bits arranged so that any small corruption sticks out and can be <b>fixed on the spot</b>, with no need to ask for a resend. Add a little redundancy and you catch errors; add more and you can correct them outright.</p><p>This is the opposite of source coding: compression <i>removes</i> waste to shrink data, then channel coding <i>adds</i> just-right waste back to armour it for the channel. It's why a scratched CD still plays, a QR code still scans with a corner missing, and a spacecraft's faint whisper still arrives readable.</p>`
});
