// dummies-bpsk-vs-dbpsk.js — plain-English "For Dummies" entry for BPSK vs DBPSK.
// Populates CONTENT_DUMMIES['bpsk-vs-dbpsk'] (defined in content.js). No jargon.
Object.assign(CONTENT_DUMMIES, {
  'bpsk-vs-dbpsk': String.raw`<p>Imagine you and a friend are flashing a torch at each other across a valley to send a secret message, and you have agreed that "torch pointing left" means the letter A and "torch pointing right" means B. This is like <b>BPSK</b>: each flash means something all on its own, but only if your friend knows which way is truly "left." If your friend is standing upside-down (or just got confused about directions), they will read every single one of your signals backwards and the whole message comes out wrong. Sorting out "which way is really left" takes a bit of fussing at the start, and if anyone ever flips, everything after that flips too.</p>
<p>Now imagine a cleverer rule: instead of caring which way the torch points, you only care whether it <b>changed</b> since the last flash. "Same as before" means A, "turned to the other side" means B. This is <b>DBPSK</b>. The beauty is that your friend no longer needs to know which way is left — they just watch for changes. Even if they are standing on their head, a change still looks like a change, so the message arrives perfectly. You skip all the fussing at the start, which is why this style is great for quick little bursts of information, like a walkie-talkie or a Wi-Fi packet that has to start and finish in a flash.</p>
<p>Of course there is a small catch, and it is a fair trade. Because your friend judges each flash by comparing it to the <b>previous</b> flash, and that previous flash might itself have been a little hard to read in the fog, they occasionally get confused — a touch more often than with the strict "which way is left" method. In fact one misread flash usually spoils <b>two letters</b> in a row, because it is used twice (once as the current signal and once as the reference for the next). So DBPSK needs just a hair more signal strength (about one notch, called a decibel) to be as reliable, but in return it is simpler, quicker to get going, and shrugs off confusion about direction. Engineers pick BPSK when they are squeezing out every last drop of range, and DBPSK when they want something cheap, fast, and forgiving.</p>`
});
