// dummies-synchronization.js — plain-English "For Dummies" entry for Synchronization (Overview).
// Populates CONTENT_DUMMIES['synchronization'] (key is the topic id). No jargon, everyday analogies.
Object.assign(CONTENT_DUMMIES, {
  'synchronization': String.raw`<p>Imagine you turn up late to a fast partner dance and try to jump straight in. Before you can actually move <b>with</b> your partner instead of just crashing into them, several separate things all have to line up, and if even one of them is off you end up stumbling around. A radio receiver faces exactly this the instant it switches on: the message is already there in the air, but the receiver has arrived "late" and knows none of the little details it needs to read it. The very first thing it must match is <b>speed</b>. The transmitter spins out its signal like a dancer twirling at a steady rate, and the receiver has its own tiny spinning clock inside. No two clocks are ever built perfectly alike, so one always turns a hair faster than the other, and that tiny mismatch makes the whole picture drift round and round forever without settling. Until the receiver nudges its own spin to match the incoming one, everything it reads keeps sliding away.</p>
<p>Even once the two are spinning at the same speed, they can still be <b>turned the wrong way</b> relative to each other, like two dancers rotating in perfect time but facing in different directions — a fixed, stubborn twist that has to be undone before their moves actually match up. Getting that angle wrong scrambles which way counts as "yes" and which way counts as "no." On top of that, the message arrives as a steady stream of little beats, and the receiver has to grab each beat at exactly the right <b>moment</b> — right in the clear middle, not the blurry edge. Snatch too early or too late and you catch each beat while it is still smeared into its neighbours, the way clapping along just off the rhythm turns a song into mush. Lining up those instants is the difference between crisp, readable beats and an indistinct blur, and it matters just as much as matching the speed.</p>
<p>Finally, suppose you have matched the speed, undone the twist, and are catching every beat cleanly — you can now hear each letter perfectly, and yet you <b>still</b> might not understand a word. Why? Because you do not know where the <b>sentences begin</b>. It is like switching on a radio play halfway through a line: every syllable is crystal clear, but until you catch the start of a phrase it is all just a run-on jumble. The receiver solves this by listening for a special agreed-upon "start here" jingle stitched into the message, a landmark that announces where each fresh chunk begins. And all of this has to happen in a sensible order — you cannot fix the angle of something that is still drifting, and you cannot find the start of words you cannot yet read cleanly — which is why a receiver tackles the biggest, fastest problems first and then works its way down to the smaller, finer ones.</p>`
});
