/* short-ldpc.js — "Single-Packet Short LDPC Codes" topic (Spread Spectrum & Coding).
   Single CONTENT.topics.push, deep schema, inline from-scratch derivations.
   THIS topic owns the SHORT-BLOCKLENGTH / SINGLE-PACKET regime (n ~ 32-1000 bits, one-shot,
   no HARQ combining, latency-critical URLLC & IoT) where the asymptotic LDPC story breaks down:
   the finite-blocklength (Polyanskiy-Poor-Verdu) benchmark, channel dispersion V and the
   sqrt(V/n) back-off, minimum distance / union-bound floors, girth / short cycles / the Moore
   tree bound, trapping & absorbing sets, protograph + quasi-cyclic circulant lifting, 5G BG2,
   near-ML decoders (OSD / GRAND), and the honest short-LDPC vs short-polar (CA-SCL) comparison.
   It deliberately COMPLEMENTS and CROSS-REFERENCES the siblings and does NOT re-derive their
   material: 'ldpc' owns the GENERAL family (Tanner graph, sum-product / min-sum belief
   propagation, degree distributions, density-evolution THRESHOLDS, asymptotic capacity-approaching
   behaviour); 'tanner-graph' owns the bipartite graph, cycles and girth machinery; 'polar-codes'
   is the short-code competitor (CA-SCL); 'shannon' / 'information-theory' own capacity. We point
   there for the BP machinery instead of repeating it.
   All text in String.raw; no literal backticks, no dollar-immediately-before-curly-brace.
   Every SVG marker/def/gradient id is prefixed "sldpc-" to avoid collisions. */
CONTENT.topics.push(
  {
    id: 'short-ldpc',
    title: 'Single-Packet Short LDPC Codes',
    category: 'Spread Spectrum & Coding',
    tags: ['short blocklength', 'single packet', 'finite blocklength', 'FBL', 'normal approximation', 'channel dispersion', 'Polyanskiy', 'URLLC', 'IoT', 'mMTC', 'BLER', 'minimum distance', 'union bound', 'error floor', 'trapping set', 'absorbing set', 'girth', 'short cycle', 'Moore bound', 'protograph', 'quasi-cyclic', 'QC-LDPC', 'circulant lifting', 'PEG', 'ACE', '5G NR', 'base graph 2', 'BG2', 'ordered statistics decoding', 'OSD', 'GRAND', 'min-sum', 'short polar', 'CA-SCL'],
    summary: String.raw`This topic is the <em>short-blocklength, single-packet</em> counterpart to the general <em>LDPC Codes</em> topic. Where that one owns the whole code family — the Tanner graph, the sum-product / min-sum belief-propagation algebra, degree distributions, and the <strong>density-evolution thresholds</strong> that describe LDPC codes as $n\to\infty$ — this topic lives entirely in the regime where that asymptotic story <strong>breaks down</strong>: blocklengths of roughly $n\sim 32$ to $1000$ bits, decoded <em>one shot</em> with no retransmission and no HARQ chase-combining or incremental redundancy across packets, under a hard latency budget. That is the world of <strong>URLLC</strong> (ultra-reliable low-latency communication, targeting about $1\,\mathrm{ms}$ and $99.999\%$ success), of <strong>IoT / mMTC</strong> short payloads and control signalling, where a single short codeword must succeed alone and the metric is the <strong>block error rate</strong> (BLER / FER), not the bit error rate. At these lengths the capacity $C$ and the belief-propagation threshold are the wrong benchmarks — they are $n\to\infty$ limits. The right benchmark is the <strong>finite-blocklength (FBL) bound</strong> of Polyanskiy, Poor and Verd&uacute; (2010), whose <em>normal approximation</em> $R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}$ shows that the achievable rate falls below capacity by a $\sqrt{V/n}$ <strong>back-off</strong> governed by the <em>channel dispersion</em> $V$ — for the real AWGN channel $C=\tfrac12\log_2(1+\mathrm{SNR})$ and $V=\tfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}(\log_2 e)^2$. That single $\sqrt{V/n}$ term is the whole story: halving $n$ multiplies the penalty by $\sqrt 2$, and the real figure of merit is the <em>gap to the FBL bound in dB at a target BLER</em>, not the gap to capacity. Different things then dominate design. <strong>Minimum distance and its multiplicity</strong> matter, because a short code has only a countable handful of low-weight codewords and the union bound $\mathrm{BLER}\approx\sum_w A_w\,Q(\sqrt{2wR\,E_b/N_0})$ actually predicts an <strong>error floor</strong>. <strong>Girth and short cycles</strong> matter, because at short $n$ there is no room to grow a cycle-free neighbourhood — the Moore / tree bound forces $n$ up sharply for any target girth — so length-4 and length-6 cycles correlate the BP messages badly. <strong>Trapping sets, near-codewords and absorbing sets</strong> matter, small sub-graphs on which BP gets stuck and which cause the floor. And the gap between BP and true maximum-likelihood decoding is <em>larger</em> at short $n$, so near-ML decoders pay off. The design levers are <strong>protograph / multi-edge-type</strong> construction, <strong>quasi-cyclic (QC)</strong> lifting of a small base graph by $Z\times Z$ circulant permutations ($n=Z\,n_b$, hardware-friendly), <strong>PEG</strong> and <strong>ACE</strong> to maximise girth and avoid small trapping/absorbing sets, and puncturing / shortening for flexibility. 5G NR uses <strong>LDPC base graph 2 (BG2)</strong> for exactly this short-block, low-rate regime (rate $\approx 1/5$ to $2/3$, info length up to $3840$) while BG1 serves long, high-rate data. Decoding keeps min-sum belief propagation with layered scheduling as the workhorse but adds near-ML post-processing — <strong>ordered-statistics decoding (OSD)</strong> of order $t$ (reprocess the most reliable positions, complexity $\sum_{i=0}^{t}\binom{k}{i}$ re-encodings) and <strong>GRAND</strong> for very short codes. Finally the honest comparison with the short-code competitor: at short blocks CRC-aided SCL <em>polar</em> is very strong, and 5G chose polar for the very-short <em>control</em> channel while LDPC (BG2) carries data even at short lengths. Throughout we reference <em>LDPC</em> and <em>Tanner Graph</em> for the belief-propagation and girth machinery, <em>Polar Codes</em> for the competitor, and <em>Shannon</em> / <em>Information Theory</em> for capacity — and keep the spotlight on what is unique to the short single packet.`,
    prerequisites: ['ldpc', 'tanner-graph', 'shannon'],
    intro: String.raw`<p><strong>Why does one short packet change the entire game?</strong> Almost everything you were taught about LDPC codes — that they come within a fraction of a decibel of the Shannon limit, that their belief-propagation decoder is near-optimal, that a well-designed degree distribution has a sharp threshold — is a statement about <em>long</em> blocks. It is the $n\to\infty$ story, and it is genuinely the reason LDPC codes carry the world's high-throughput data links. But there is a growing and unforgiving corner of communication where none of that applies: a single, short, must-not-fail message of a few dozen to a few hundred bits, decoded once, right now, with no second chance. A factory robot's emergency-stop command. A vehicle-to-vehicle collision warning. A sensor waking up to send eight bytes and going back to sleep. A control message that tells your phone how to connect before any data flows. These are <em>single packets</em>, and for them the asymptotic story does not just weaken — it actively misleads. This topic is about what really governs a short LDPC code, and it is a different set of things.</p>
<p>Keep the division of labour in mind from the start, because it is the whole reason this topic exists alongside the general <em>LDPC Codes</em> topic. That sibling owns the <strong>code family</strong>: how to read the sparse parity-check matrix as a Tanner graph, how the sum-product and min-sum messages flow along its edges, how a degree distribution is optimised, and how <em>density evolution</em> predicts the exact noise threshold below which the error probability drives to zero as the block grows without bound. We will lean on all of that machinery and never re-derive it — when we need belief propagation we point there, and when we need the bipartite graph and its cycles we point to <em>Tanner Graph</em>. What <em>this</em> topic owns is everything that is peculiar to the <strong>single short packet</strong>: why the finite-blocklength bound, not capacity, is the yardstick; why the square-root-of-dispersion back-off dominates; why a short code's countable low-weight codewords produce an error floor the asymptotic theory never sees; why short cycles and trapping sets bite so hard when there is no room to avoid them; and why, at these lengths, it is worth reaching past belief propagation toward maximum-likelihood.</p>
<p>The single idea that reorganises everything is this: <strong>at short $n$, randomness has not had time to average out.</strong> Shannon's capacity, and the LDPC threshold that mirrors it, are laws of large numbers — they describe the typical behaviour of an enormous codeword whose thousands of coordinates each nudge the outcome a little. Shrink the codeword to a hundred bits and the fluctuations no longer wash away; the outcome of a single transmission is dominated by the <em>variance</em> of the per-symbol information, a quantity called the <strong>channel dispersion</strong> $V$. Polyanskiy, Poor and Verd&uacute; made this precise in 2010: the maximum rate at which you can transmit $n$ symbols with block error probability $\epsilon$ is approximately $C-\sqrt{V/n}\,Q^{-1}(\epsilon)$, the capacity minus a back-off that scales as $1/\sqrt n$. At $n=128$ and a reliability of $10^{-5}$ that back-off can eat more than half of capacity. No amount of clever coding closes it — it is a property of the channel and the blocklength, the fundamental wall a short code lives against. The job of a good short LDPC code is not to approach capacity; it is to approach that wall.</p>
<p>This topic builds the short-packet picture from the ground up. We fix the single-packet setting and its BLER metric; we replace capacity with the FBL normal approximation and give the dispersion $V$ for the AWGN, BSC and BEC channels; we define the gap-to-the-FBL-bound in dB as the true figure of merit; we show how minimum distance and multiplicity produce a union-bound error floor; we derive the Moore / tree bound that makes large girth impossible at short $n$; we explain trapping and absorbing sets as the mechanism of the floor; we lay out the design levers — protographs, quasi-cyclic circulant lifting, PEG and ACE, and 5G's base graph 2; we survey the decoders that matter here, from layered min-sum to ordered-statistics decoding and GRAND; and we put short LDPC honestly beside short polar (CA-SCL). Throughout we cross-reference <em>LDPC</em> and <em>Tanner Graph</em> for the belief-propagation and girth machinery, <em>Polar Codes</em> for the competitor, and <em>Shannon</em> and <em>Information Theory</em> for the capacity those short packets can never quite reach.</p>`,
    sections: [
      {
        h: 'Why one short packet breaks the asymptotic story',
        html: String.raw`<p><strong>Why can't we just use the ordinary LDPC theory at short lengths?</strong> Because the ordinary theory is built on limits that a hundred-bit packet never reaches. The two headline promises of LDPC codes — near-capacity performance and near-optimal belief-propagation decoding — are both asymptotic. Capacity $C$ is the largest rate at which the error probability can be driven to zero <em>as the blocklength grows without bound</em>; the density-evolution threshold that the general <em>LDPC Codes</em> topic derives is likewise the noise level below which a random code from a given degree distribution has vanishing error <em>as $n\to\infty$</em>. Both are statements about an infinitely long codeword. Ask them about a single short packet and they answer a question you did not pose.</p>
        <p>The reason is statistical. Decoding success on a memoryless channel is governed by the accumulated <em>information density</em> across the $n$ received symbols — a sum of $n$ random contributions. By the law of large numbers that sum concentrates around $nC$ when $n$ is huge, so a rate just below $C$ almost surely succeeds; that concentration <em>is</em> the capacity theorem. But concentration is exactly what a short block lacks. At $n=100$ the sum has visible variance, and whether a given packet decodes depends on which side of the mean its particular noise realisation lands. The relevant number is no longer just the <em>mean</em> per-symbol information ($C$) but its <em>variance</em>, the channel dispersion $V$ — and a whole second-order theory grows out of that variance.</p>
        <div class="callout"><strong>Framing.</strong> This topic owns the <em>single short packet</em>: the finite-blocklength benchmark and dispersion, the BLER metric, minimum-distance and union-bound floors, girth and the Moore bound, trapping / absorbing sets, protograph and quasi-cyclic construction, 5G base graph 2, and near-ML decoders (OSD, GRAND). It deliberately does <em>not</em> re-derive the belief-propagation machinery — the sum-product and min-sum message rules, the check-node $\tanh$ update, density evolution and the threshold — which are the general <em>LDPC Codes</em> topic's subject, nor the bipartite graph, cycles and girth definitions, which are <em>Tanner Graph</em>'s. We name them and point there. We also lean on <em>Shannon</em> and <em>Information Theory</em> for capacity $C$, and on <em>Polar Codes</em> for the short-code competitor CA-SCL.</p></div>
        <p>One contrast frames the whole topic. In the <em>asymptotic</em> regime the design goal is a good <em>threshold</em>: shape the degree distribution so the BP decoder works as close to capacity as possible, and trust that a long enough block will inherit that promise. In the <em>short single-packet</em> regime the threshold is almost irrelevant; the design goals become a large <em>minimum distance</em> with small multiplicity, a healthy <em>girth</em> free of small trapping sets, and a decoder strong enough to close the enlarged gap to maximum likelihood. Same code family, opposite priorities — and the rest of this topic is that reversal, made quantitative.</p>`
      },
      {
        h: 'The single-packet setting: URLLC, IoT, and no second chance',
        html: String.raw`<p>The short-blocklength regime is not an academic curiosity; it is defined by a set of real services whose requirements forbid the long-block tricks. Three families dominate.</p>
        <ul>
          <li><strong>URLLC — ultra-reliable low-latency communication.</strong> The 5G design point is roughly a $1\,\mathrm{ms}$ user-plane latency at a residual error of $10^{-5}$ or better ($99.999\%$ reliability), for payloads of tens of bytes: industrial control, motion control, tactile/haptic feedback, vehicle-to-everything safety. The latency budget alone rules out long codewords (a long block takes too long to fill and to interleave) and rules out retransmission (a round trip blows the budget). The packet must be short <em>and</em> succeed on the first try.</li>
          <li><strong>IoT / mMTC — massive machine-type communication.</strong> Vast numbers of cheap, battery-limited sensors send tiny payloads infrequently. Here the packet is short because the <em>message</em> is short (a temperature, a meter reading, an alarm), and the device cannot afford the energy or the airtime of long codes, deep interleavers or many retransmissions. NB-IoT control and small-data transfers live here.</li>
          <li><strong>Control signalling.</strong> The messages that run the link itself — scheduling grants, acknowledgements, connection setup — are intrinsically short, frequent, and latency-critical, and a lost one is expensive.</li>
        </ul>
        <p>The unifying constraint is <strong>one shot</strong>. There is no HARQ chase-combining and no incremental-redundancy retransmission that a later theory could fold in: either there is genuinely no time or energy for a second transmission, or we are analysing the performance of the <em>first</em> transmission on its own. A single codeword of length $n$ must carry the whole burden. That has two immediate consequences that colour every later section.</p>
        <div class="callout tip"><strong>Two consequences of one shot.</strong> First, the metric is the <strong>block (frame) error rate</strong>, BLER (equivalently FER), not the bit error rate: a control command or a sensor reading is right or wrong as a <em>unit</em>, and a single surviving bit error ruins the packet. Second, there is no averaging <em>across</em> packets — no combining of energy or redundancy from a retransmission — so the only averaging available is <em>within</em> the one short block, which is precisely the averaging the law of large numbers cannot complete at small $n$. Every quantity in this topic (the dispersion back-off, the union-bound floor, the trapping-set failure) is a within-one-packet effect.</p></div>
        <p>It is worth being explicit about what "short" means numerically, because it sets the whole scale. This topic targets $n$ from about $32$ bits (the shortest control words) through a few hundred (typical URLLC payloads) up to around $1000$; below and around this range the finite-blocklength effects are dominant. By contrast the LDPC codes in the general topic routinely run to $n$ of many thousands, where the asymptotic theory is an excellent guide. The transition is not sharp, but the change of priorities is real, and it happens well inside the range where LDPC codes are still used.</p>`
      },
      {
        h: 'Finite blocklength: replacing capacity with the PPV bound',
        html: String.raw`<p>If capacity is the wrong yardstick at short $n$, what is the right one? The answer is the <strong>finite-blocklength (FBL) bound</strong> introduced by Polyanskiy, Poor and Verd&uacute; in 2010, which characterises the maximum coding rate $R^*(n,\epsilon)$ achievable with a length-$n$ code at block error probability $\epsilon$. Unlike capacity it is a function of <em>both</em> $n$ and $\epsilon$, and it is what a short code should actually be measured against. Its practically indispensable form is the <strong>normal approximation</strong>:</p>
        <p>$$ R^*(n,\epsilon)\ \approx\ C\ -\ \sqrt{\frac{V}{n}}\;Q^{-1}(\epsilon)\ +\ \frac{\log_2 n}{2n}, $$</p>
        <p>where $C$ is the capacity, $V$ is the <em>channel dispersion</em>, $Q^{-1}$ is the inverse Gaussian tail (so $Q^{-1}(\epsilon)>0$ for small $\epsilon$), and the final $\tfrac{\log_2 n}{2n}$ is a smaller third-order correction. Read it as three pieces: you start from capacity, you <em>subtract</em> a back-off that shrinks like $1/\sqrt n$, and you add a small positive nudge. The middle term is everything.</p>
        <p>The origin of the middle term is the same law-of-large-numbers argument the previous section flagged, made quantitative. The information density accumulated over $n$ symbols has mean $nC$ and standard deviation $\sqrt{nV}$; by the central limit theorem it is approximately Gaussian, and demanding that it exceed $nR$ with probability $1-\epsilon$ forces $nR\lesssim nC-\sqrt{nV}\,Q^{-1}(\epsilon)$, i.e. $R\lesssim C-\sqrt{V/n}\,Q^{-1}(\epsilon)$. The dispersion $V$ is literally the variance of the per-symbol information — the sibling <em>Information Theory</em> topic's mutual-information quantity, now taken to second order.</p>
        <div class="callout"><strong>How brutal the back-off is.</strong> Take the real AWGN channel at $\mathrm{SNR}=1$ ($0\,\mathrm{dB}$), so $C=\tfrac12\log_2 2=0.5$ bits/use and (next section) $V\approx 0.78$. At $\epsilon=10^{-5}$ we have $Q^{-1}(10^{-5})\approx 4.265$. Then the back-off is $\sqrt{V/n}\,Q^{-1}(\epsilon)$: at $n=1000$ it is $0.119$ bits (so $R^*\approx 0.386$, already $23\%$ below capacity); at $n=128$ it is $0.333$ bits (so $R^*\approx 0.194$, more than $60\%$ below capacity); at $n=64$ it is $0.471$ bits and $R^*$ has nearly collapsed. The message is stark: at a hundred bits and five-nines reliability, <em>capacity is roughly twice the rate you can actually use</em>, no matter how good your code. The first diagram plots exactly this rate-versus-$n$ climb toward $C$.</p></div>
        <p>This is why "we are $0.5\,\mathrm{dB}$ from capacity", the proud long-block slogan, is meaningless for a single short packet: capacity is unreachable by a wide, calculable margin that has nothing to do with the code. The honest question is how close a code gets to $R^*(n,\epsilon)$ — the FBL bound — which we turn into a decibel figure of merit two sections on. First we need $V$.</p>`
      },
      {
        h: 'Channel dispersion V for AWGN, BSC and BEC',
        html: String.raw`<p>The back-off is governed entirely by the <strong>channel dispersion</strong> $V$, the variance of the per-symbol information density (in $\mathrm{bits}^2$ per channel use). It is as fundamental to the finite-blocklength theory as capacity is to the asymptotic theory, and it has clean closed forms for the standard channels.</p>
        <p><strong>Real AWGN channel.</strong> With signal-to-noise ratio $\mathrm{SNR}$,</p>
        <p>$$ C=\tfrac12\log_2\!\left(1+\mathrm{SNR}\right),\qquad V=\frac{\mathrm{SNR}\,(\mathrm{SNR}+2)}{2\,(1+\mathrm{SNR})^{2}}\,(\log_2 e)^{2}\ \ \mathrm{bits}^2/\text{use}. $$</p>
        <p>At $\mathrm{SNR}=1$ this gives $C=0.5$ and $V=\tfrac{1\cdot 3}{2\cdot 4}(\log_2 e)^2=0.375\cdot(1.4427)^2\approx 0.781$. As $\mathrm{SNR}\to\infty$, $V\to \tfrac12(\log_2 e)^2\approx 1.04$ (a ceiling), and as $\mathrm{SNR}\to 0$, $V\to 0$ — a very noisy channel has both small capacity and small dispersion.</p>
        <p><strong>Binary symmetric channel BSC$(p)$.</strong></p>
        <p>$$ C=1-H_2(p),\qquad V=p(1-p)\,\log_2^{2}\!\frac{1-p}{p}\ \ \mathrm{bits}^2/\text{use}, $$</p>
        <p>with $H_2$ the binary entropy. For $p=0.11$, $C\approx 0.5$ and $V\approx 0.89$: a BSC with the same capacity as the AWGN example above has a <em>larger</em> dispersion, so its short-block back-off is worse — dispersion, not capacity, decides who suffers most at short $n$.</p>
        <p><strong>Binary erasure channel BEC$(\epsilon_e)$.</strong> The information density is Bernoulli — a clean $1$ bit when a symbol survives, $0$ when it is erased — so</p>
        <p>$$ C=1-\epsilon_e,\qquad V=\epsilon_e\,(1-\epsilon_e)\ \ \mathrm{bits}^2/\text{use}. $$</p>
        <p>For $\epsilon_e=0.5$, $C=0.5$ and $V=0.25$ — the smallest dispersion of the three at equal capacity, so the BEC is the <em>friendliest</em> channel at short blocklength. Ordering the three at $C=0.5$: BEC ($V=0.25$) is easiest, AWGN ($V=0.78$) is middling, BSC ($V=0.89$) is hardest.</p>
        <table class="data">
          <tr><th>Channel</th><th>Capacity $C$ (bits/use)</th><th>Dispersion $V$ (bits$^2$/use)</th><th>Value at $C=0.5$</th><th>Short-$n$ back-off</th></tr>
          <tr><td>Real AWGN, SNR $s$</td><td>$\tfrac12\log_2(1+s)$</td><td>$\dfrac{s(s+2)}{2(1+s)^2}(\log_2 e)^2$</td><td>$V\approx 0.78$ ($s=1$)</td><td>middling</td></tr>
          <tr><td>BSC$(p)$</td><td>$1-H_2(p)$</td><td>$p(1-p)\log_2^2\tfrac{1-p}{p}$</td><td>$V\approx 0.89$ ($p=0.11$)</td><td>largest (worst)</td></tr>
          <tr><td>BEC$(\epsilon_e)$</td><td>$1-\epsilon_e$</td><td>$\epsilon_e(1-\epsilon_e)$</td><td>$V=0.25$ ($\epsilon_e=0.5$)</td><td>smallest (best)</td></tr>
        </table>
        <p>The single lesson is that dispersion, the second-order statistic, is the quantity a short-packet designer must watch. Two channels can share a capacity yet impose very different short-block penalties, and only $V$ tells them apart. Everything downstream — how much rate you must sacrifice, how many dB you are from the wall — flows from $V$ through the $\sqrt{V/n}$ term.</p>`
      },
      {
        h: 'The real figure of merit: gap to the FBL bound in dB',
        html: String.raw`<p>Because the FBL bound $R^*(n,\epsilon)$ sits a calculable distance below capacity, quoting a code's <em>gap to capacity</em> at short $n$ is misleading — most of that gap is the unavoidable dispersion back-off, not a deficiency of the code. The meaningful quantity is the <strong>gap to the finite-blocklength bound</strong>, and the field states it in decibels at a fixed target BLER.</p>
        <p>Concretely: fix a rate $R$, a blocklength $n$ and a target BLER $\epsilon$. Find the SNR at which the FBL bound just permits $(R,n,\epsilon)$ — call it $\mathrm{SNR}_{\text{FBL}}$ — and the SNR at which the actual code achieves the same $(R,n,\epsilon)$ in simulation, $\mathrm{SNR}_{\text{code}}$. The figure of merit is</p>
        <p>$$ \Delta_{\mathrm{dB}}=10\log_{10}\!\frac{\mathrm{SNR}_{\text{code}}}{\mathrm{SNR}_{\text{FBL}}}\ \ \ (\text{at fixed }R,\,n,\,\epsilon). $$</p>
        <p>A good short LDPC or short polar code under strong decoding sits a few tenths of a dB to about a dB from the FBL bound; that small, honest number is what tells you the code is nearly the best a length-$n$ one-shot code could be. Comparing instead to capacity would report a "gap" of several dB that no code can close.</p>
        <div class="callout tip"><strong>Two very different gaps.</strong> For $R=0.5$, $\epsilon=10^{-5}$ on AWGN, the FBL bound itself lies below capacity by about $1.34\,\mathrm{dB}$ at $n=1000$, and by about $3.41\,\mathrm{dB}$ at $n=128$ (worked in the numericals). Those decibels are the <em>gap of the bound to capacity</em> — the wall's distance from the mirage. A real code adds its own smaller gap <em>on top</em> of the bound. Reporting the sum as "distance from capacity" hides which part is physics ($\sqrt{V/n}$, uncloseable) and which part is engineering (the code, improvable). The dB gap to the FBL bound isolates the engineering.</p></div>
        <p>This reframing quietly changes the design target. In the long-block world you chase capacity and every tenth of a dB is a code-design win. In the short-block world capacity is fenced off, and the contest is to hug the FBL bound. That is why short-packet research is full of comparisons of short LDPC, short polar, short BCH-with-OSD and random-like codes against the <em>same</em> PPV curve — the bound is the shared referee, and the winner is whoever leaves the smallest $\Delta_{\mathrm{dB}}$.</p>`
      },
      {
        h: 'What dominates: minimum distance, multiplicity, and the union bound',
        html: String.raw`<p>In the asymptotic theory the minimum distance barely features — density evolution talks about thresholds, not distances, and a random long LDPC code's few low-weight codewords are drowned out. At short $n$ the opposite is true: a short code has only a <em>countable, small</em> set of low-weight codewords, and those few codewords dominate the error rate at the high-SNR end. The tool is the classical <strong>union bound</strong>.</p>
        <p>Let $A_w$ be the number of codewords of Hamming weight $w$ (the weight enumerator), $d_{\min}$ the minimum distance, and $R$ the rate. On the AWGN/BPSK channel the block error probability under maximum-likelihood decoding is bounded by</p>
        <p>$$ \mathrm{BLER}\ \le\ \sum_{w\ge d_{\min}} A_w\,Q\!\left(\sqrt{2wR\,E_b/N_0}\right)\ \approx\ A_{d_{\min}}\,Q\!\left(\sqrt{2\,d_{\min}\,R\,E_b/N_0}\right), $$</p>
        <p>the sum being dominated at high SNR by its smallest-weight term. Two things jump out. First, $d_{\min}$ sets the <em>slope</em> of the error curve: a larger minimum distance steepens the waterfall, because the $Q$-function argument grows with $\sqrt{d_{\min}}$. Second, the <em>multiplicity</em> $A_{d_{\min}}$ sets the <em>height</em>: many minimum-weight codewords lift the whole curve. A short code with a mediocre $d_{\min}$ and a large multiplicity displays a visible <strong>error floor</strong> — the curve stops falling steeply and flattens onto the union-bound line.</p>
        <div class="callout"><strong>Why this is a short-code disease.</strong> The union bound is only tight, and only visible, when the number of low-weight codewords is small enough that they do not blur into a continuum — precisely the short-code situation. At long $n$ the weight spectrum is dense and the waterfall is governed by the threshold long before any single codeword matters; the floor, if any, sits at astronomically low BLER. At short $n$ the handful of minimum-weight codewords sit right in the operating range, so maximising $d_{\min}$ and <em>minimising its multiplicity</em> becomes a first-order design goal — the reverse of the asymptotic mindset, where distance is an afterthought.</p></div>
        <p>This is also why belief propagation is not the last word at short $n$. BP does not achieve the union-bound (maximum-likelihood) performance; it fails earlier, on sub-graph structures we meet next. So a short code has <em>two</em> error mechanisms stacked up: the ML floor set by $d_{\min}$ and $A_{d_{\min}}$, and an additional, higher BP floor set by trapping sets. Closing the gap between them is exactly what the near-ML decoders later in the topic are for. The third diagram sketches both the shallow short-$n$ waterfall and the floor they produce.</p>`
      },
      {
        h: 'Girth, short cycles, and the Moore tree bound',
        html: String.raw`<p>Belief propagation is <em>exact</em> only on a cycle-free graph (a tree); every cycle in the Tanner graph feeds a message back to a node that already influenced it, correlating information that BP assumes independent. The shortest cycle length — the <strong>girth</strong>, always even and at least $4$ — therefore controls how badly BP is biased. The definitions and the cycle-hunting belong to the <em>Tanner Graph</em> topic; what is special at short $n$ is that <em>you cannot make the girth large even if you want to</em>.</p>
        <p>The obstruction is a counting bound. Root a tree at a variable node of a $(d_v,d_c)$-regular Tanner graph and expand it outward; to reach a given girth $g$ without any cycle closing, all the variable nodes encountered up to depth $g/2$ must be distinct. Counting them gives the <strong>Moore / tree bound</strong> on the number of variable nodes:</p>
        <p>$$ n\ \ge\ 1+d_v(d_c-1)\sum_{i=0}^{(g-6)/2}\big[(d_v-1)(d_c-1)\big]^{i}. $$</p>
        <p>For the classic rate-$\tfrac12$ $(d_v,d_c)=(3,6)$ ensemble this reads: girth $\ge 6$ needs $n\ge 16$; girth $\ge 8$ needs $n\ge 166$; girth $\ge 10$ needs $n\ge 1666$; girth $\ge 12$ needs $n\ge 16666$. The blocklength required for each extra rung of girth grows by roughly a factor of $(d_v-1)(d_c-1)=10$ — <em>exponentially</em>. A $128$-bit code physically cannot carry a $(3,6)$ graph of girth $8$; it is boxed into girth $6$ at best, so it must live with short cycles that a long code would design away.</p>
        <div class="callout tip"><strong>The short-code squeeze.</strong> Long codes get girth almost for free — at $n=10{,}000$ even girth $10$ is comfortably feasible — so BP is nearly exact and the asymptotic theory holds. Short codes are squeezed: the same Moore bound that is generous at $n=10^4$ is a hard ceiling at $n=10^2$. Every length-$4$ and length-$6$ cycle that a short code is forced to contain is a place where BP messages become correlated and the decoder loses ground to maximum likelihood. This is a purely combinatorial short-$n$ effect — nothing about the channel, everything about there being too few nodes to spread the edges out.</p></div>
        <p>The practical response is to spend the little girth you can afford wisely: use construction algorithms (PEG, ACE, later) that maximise the girth for the given $n$ and, more importantly, kill the <em>specific</em> small cycle clusters that trap the decoder. Girth is a blunt summary; the sharp object is the trapping set.</p>`
      },
      {
        h: 'Trapping sets, absorbing sets, and the error floor',
        html: String.raw`<p>Not all short cycles are equally harmful. The damaging structures are small sub-graphs on which belief propagation, once it starts believing a wrong pattern, <em>reinforces</em> that belief instead of correcting it. These go by several overlapping names — <strong>trapping sets</strong>, <strong>near-codewords</strong>, and the important stable subclass, <strong>absorbing sets</strong> — and they are the microscopic cause of the LDPC <strong>error floor</strong>.</p>
        <p>A trapping set is a small set of variable nodes that, when in error, leaves only a few unsatisfied check nodes, and those few checks cannot muster enough corrective evidence to flip the errant bits back. An $(a,b)$ trapping set has $a$ erroneous variable nodes and $b$ unsatisfied checks; the dangerous ones have small $a$ and very small $b$. An <em>absorbing set</em> is the especially stable case where every erroneous variable node has strictly more satisfied than unsatisfied neighbouring checks, so the local majority actively holds the error in place — BP settles into it and stays. Because these sets involve only a handful of nodes, they are triggered by modest noise and produce residual errors at BLERs well above the ML floor: a shallow, flattening tail in the waterfall.</p>
        <div class="callout"><strong>Why the floor is a single-packet problem.</strong> A trapping set is a fixed feature of the graph; whether it causes a failure depends on the noise landing on those specific few bits in a single transmission. Over one short packet, with no averaging across retransmissions and no long-block dilution, that unlucky landing is not rare enough — it surfaces as an error floor right in the operating range of URLLC's $10^{-5}$ to $10^{-9}$ targets. At long $n$ the same sets exist but sit far below any BLER of interest. So the floor, like the union-bound floor, is a disease of the short single packet, and for the same underlying reason: too little averaging.</p></div>
        <p>The design countermeasures are therefore aimed squarely at these sets. Girth-maximising construction (PEG) raises the size of the smallest cycles; the <strong>ACE</strong> criterion (approximate cycle extrinsic-message degree) specifically ensures that short cycles are well connected to the rest of the graph so they cannot self-isolate into an absorbing set; and decoder-side <em>post-processing</em> detects a stalled trapping-set pattern and perturbs the messages to break out of it. All three are short-code refinements — at long $n$ nobody needs them. The detailed message-update rules they perturb are, again, the general <em>LDPC Codes</em> topic's; here they are the levers, not the derivation.</p>`
      },
      {
        h: 'Design levers: protographs, quasi-cyclic lifting, PEG and ACE, 5G BG2',
        html: String.raw`<p>How do you actually build a short LDPC code that has a large minimum distance, a healthy girth, and no small absorbing sets, and that a chip can decode fast? The modern answer is almost always the same pipeline: design a tiny <em>base graph</em>, then <em>lift</em> it with structured permutations. This is where short-code construction concentrates.</p>
        <p><strong>Protograph / multi-edge-type construction.</strong> Start from a very small Tanner graph — the <em>protograph</em> — with $m_b$ check nodes and $n_b$ variable nodes and possibly parallel edges. It fixes the code's degree profile and, through density-evolution analysis of the protograph, its asymptotic behaviour, while remaining small enough to reason about by hand. The protograph encodes the code's "DNA".</p>
        <p><strong>Quasi-cyclic (QC) lifting.</strong> Replace every edge of the protograph by a $Z\times Z$ <em>circulant permutation matrix</em> $\mathbf P^{s}$ (a cyclic shift by $s$), and every zero by a $Z\times Z$ zero block. The lifted parity-check matrix is an array of circulants, giving a code of length</p>
        <p>$$ n=Z\,n_b,\qquad m=Z\,m_b,\qquad k=n-m,\qquad R=1-\frac{m_b}{n_b}. $$</p>
        <p>For a $4\times 8$ base graph lifted by $Z=64$ you get $n=512$, $m=256$, $k=256$, rate $\tfrac12$ — a concrete short QC-LDPC code. Quasi-cyclic structure is beloved in hardware because the circulants let $Z$ bits be processed in parallel and stored compactly, and the shift values are just small integers. The second diagram draws this protograph-to-circulant lift.</p>
        <p><strong>PEG and ACE.</strong> The freedom left after fixing the protograph is the choice of shift values $s_{ij}$; that is what determines the girth and the trapping-set profile of the actual lifted code. <em>Progressive Edge-Growth (PEG)</em> adds edges one at a time, each time choosing the connection that pushes the local girth as high as possible. The <em>ACE</em> criterion additionally rejects shifts that would create small, poorly-connected cycle clusters (candidate absorbing sets). Together they turn a good base graph into a good short code.</p>
        <p><strong>5G NR base graph 2 (BG2).</strong> 5G standardised exactly this recipe with <em>two</em> protographs, and the short-block one is BG2.</p>
        <table class="data">
          <tr><th>Property</th><th>Base Graph 1 (BG1)</th><th>Base Graph 2 (BG2)</th></tr>
          <tr><td>Target regime</td><td>long blocks, high rate</td><td><strong>short blocks, low rate</strong></td></tr>
          <tr><td>Base size (rows &times; cols)</td><td>$46\times 68$</td><td>$42\times 52$</td></tr>
          <tr><td>Systematic (info) columns</td><td>$22$</td><td>$10$</td></tr>
          <tr><td>Design rate range</td><td>$1/3$ to $8/9$</td><td>$1/5$ to $2/3$</td></tr>
          <tr><td>Max info block $K$</td><td>$8448=22\cdot 384$</td><td>$3840=10\cdot 384$</td></tr>
          <tr><td>Lift set $Z$</td><td colspan="2">$51$ values of the form $a\cdot 2^{j}$, from $2$ up to $384$</td></tr>
        </table>
        <p>BG2 is the short-packet workhorse: a lower mother rate ($1/5$, more built-in redundancy) suits the low-rate, high-reliability operating points of small payloads, and its smaller base graph keeps short codes clean. The two leftmost systematic columns are always punctured for rate flexibility, and shortening / puncturing plus a CRC bend the lifted code to the exact $(n,k)$ requested. When a 5G data block is short and low-rate, it is BG2 doing the work; BG1 takes over for long, high-rate blocks. For the message-passing decoder that runs on either graph, see <em>LDPC</em>.</p>`
      },
      {
        h: 'Decoding short codes: layered min-sum, OSD, and GRAND',
        html: String.raw`<p>The workhorse decoder does not change at short $n$ — it is still belief propagation, in its hardware-friendly <em>min-sum</em> form with an offset or normalisation correction, and usually with <em>layered</em> scheduling that updates check groups sequentially so the code converges in fewer iterations. That algebra (the check-node $\tanh$/min-sum rule, the variable-node sum, the LLR initialisation) is derived in the general <em>LDPC Codes</em> topic and we do not repeat it. What changes at short $n$ is that BP alone leaves money on the table, because the gap between BP and maximum-likelihood decoding is <em>wider</em> here — so the short-code toolbox adds near-ML methods on top.</p>
        <p><strong>Ordered-statistics decoding (OSD).</strong> The most important near-ML technique for short codes. Rank the received positions by reliability, take the $k$ most reliable independent positions as a temporary information set, and <em>re-encode</em> from them; then improve that first guess by flipping small patterns of up to $t$ of those reliable positions and re-encoding each time, keeping the most likely resulting codeword. Order $t$ tests all error patterns of weight $\le t$ over the $k$ reliable bits, so the number of re-encodings is</p>
        <p>$$ N_{\text{TEP}}(t)=\sum_{i=0}^{t}\binom{k}{i}. $$</p>
        <p>For a $(128,64)$ code ($k=64$): order $1$ costs $1+64=65$ re-encodings; order $2$ costs $1+64+2016=2081$; order $3$ jumps to $43{,}745$. Even modest orders approach ML performance on short codes, which is why OSD (and its many ordered-reliability variants) is the standard way to show a short code hugging the FBL bound — at a complexity that is affordable precisely because $k$ is small.</p>
        <p><strong>GRAND.</strong> For <em>very</em> short, high-rate codes, Guessing Random Additive Noise Decoding inverts the problem: instead of decoding the codeword, guess the <em>noise</em> patterns in order of likelihood, subtract each from the received word, and stop when the result is a codeword. GRAND is near-ML for any short code (it needs only a codebook membership test, not a special structure) and is efficient exactly when the number of likely noise patterns is small — the short, high-rate regime.</p>
        <div class="callout tip"><strong>Why pay for near-ML at short $n$ but not long $n$?</strong> Two reasons converge. First, the <em>benefit</em> is larger: the BP-to-ML gap widens at short $n$ (forced short cycles and trapping sets), so there is more to gain. Second, the <em>cost</em> is smaller: OSD complexity scales with $\binom{k}{t}$ and GRAND with the number of likely noise patterns, both cheap when $k$ and $n$ are small. At long $n$ the gap is tiny and the combinatorics explode, so plain BP wins. The short single packet is the sweet spot for near-ML decoding — and, with post-processing to knock down the trapping-set floor, it is how short LDPC codes get within a fraction of a dB of the FBL bound.</p></div>`
      },
      {
        h: 'Short LDPC versus short polar (CA-SCL): the honest comparison',
        html: String.raw`<p>At short blocklengths LDPC codes are not unopposed; their principal rival is the polar code with CRC-aided successive-cancellation list decoding, the subject of the <em>Polar Codes</em> topic. The comparison matters because 5G actually deploys <em>both</em>, splitting them by role, and it is worth stating honestly rather than as a partisan claim.</p>
        <p>The headline fact is that <em>both</em> can be pushed very close to the FBL bound at short lengths when paired with a strong decoder — CA-SCL for polar, OSD or a well-designed BP for LDPC — so neither is categorically "better". They differ in the shape of their strengths.</p>
        <table class="data">
          <tr><th>Aspect</th><th>Short LDPC (QC, BG2)</th><th>Short polar (CA-SCL)</th></tr>
          <tr><td>Near-FBL at short $n$?</td><td>yes, with OSD / strong BP</td><td>yes, with CRC-aided list</td></tr>
          <tr><td>Decoder</td><td>layered min-sum BP (parallel); OSD for near-ML</td><td>successive-cancellation list (sequential) + CRC</td></tr>
          <tr><td>Latency</td><td>low (parallel message passing)</td><td>higher (serial list decoding)</td></tr>
          <tr><td>Error floor</td><td>possible (trapping / absorbing sets)</td><td>essentially none</td></tr>
          <tr><td>Very short, low rate</td><td>good, but floors can appear</td><td><strong>excellent</strong> (steep, no floor)</td></tr>
          <tr><td>Rate / length flexibility</td><td>native via lifting + puncturing / shortening</td><td>via rate matching from a $2^n$ mother</td></tr>
          <tr><td>5G NR role</td><td><strong>data</strong> channels (DL-SCH / UL-SCH), incl. short blocks via BG2</td><td><strong>control</strong> channels (PBCH / PDCCH / PUCCH), very short</td></tr>
        </table>
        <p>The 5G verdict is the practical summary. For the <em>very short</em> control messages — tens of bits, needing a clean steep curve with no floor at extreme reliability — 5G chose <strong>polar + CA-SCL</strong>. For the <em>data</em> channels, including short and low-rate transport blocks, 5G chose <strong>LDPC</strong>, using base graph 2 in exactly the short-block regime this topic is about, because LDPC's highly parallel decoder gives the throughput and low latency the data path needs and its floors can be engineered down. So the two short-code families are not really competitors so much as specialists: polar where the block is tiniest and a floor is intolerable, LDPC where parallel throughput and flexibility dominate. For the CA-SCL machinery behind the polar column, see <em>Polar Codes</em>; for the turbo family that preceded both at moderate lengths, see <em>Turbo Codes</em>.</p>`
      },
      {
        h: 'Applications and standards',
        html: String.raw`<p>The short single-packet regime is not a niche; it is where a large share of new wireless traffic lives, and short LDPC codes appear across it.</p>
        <ul>
          <li><strong>5G NR data, including URLLC.</strong> LDPC base graph 2 carries short, low-rate transport blocks on the shared data channels, and URLLC's tens-of-bytes, $1\,\mathrm{ms}$, $10^{-5}$ traffic is served here with short codewords and no reliance on retransmission. This is the flagship deployment of the whole topic.</li>
          <li><strong>NB-IoT / mMTC.</strong> Massive machine-type traffic — sensors, meters, trackers — sends tiny payloads from cheap, battery-limited devices, exactly the short-packet, energy-constrained corner where short codes and low decoding complexity are essential.</li>
          <li><strong>WiFi (IEEE 802.11n/ac/ax).</strong> The optional LDPC modes use short-to-moderate QC-LDPC codes, and short packets (acknowledgements, control frames, small data) exercise the short-block regime.</li>
          <li><strong>DVB and satellite.</strong> While DVB-S2's LDPC codes are long, the broader satellite and deep-space community studies short LDPC and short protograph codes for command and telemetry links where latency and packet size are constrained.</li>
          <li><strong>Wireless sensor networks and control links.</strong> Any system where a short, latency-critical command or reading must survive one transmission is a customer for well-designed short codes.</li>
        </ul>
        <p>The through-line is that these applications are defined by the packet being short and the transmission being one-shot, which is precisely why the finite-blocklength benchmark, the union-bound floor, the trapping-set problem and the near-ML decoders of this topic — rather than the asymptotic threshold theory — are the tools an engineer reaches for. The general <em>LDPC Codes</em> topic tells you how belief propagation works; this topic tells you what to worry about when the block is short and there is no second chance.</p>`
      },
      {
        h: 'What you should now understand',
        html: String.raw`<p>This topic is the short-blocklength, single-packet counterpart to the general <em>LDPC Codes</em> topic — the regime where the asymptotic threshold story breaks down and a different set of effects takes over. You should now be able to say:</p>
        <ul>
          <li><strong>The setting:</strong> one short packet ($n\sim 32$ to $1000$ bits), decoded one-shot with no HARQ combining or retransmission, under a latency budget — URLLC ($\sim 1\,\mathrm{ms}$, $99.999\%$), IoT/mMTC, control signalling — with <strong>BLER/FER</strong>, not BER, as the metric.</li>
          <li><strong>Why asymptotics fail:</strong> capacity $C$ and the density-evolution/BP threshold are $n\to\infty$ limits; at short $n$ the benchmark is the finite-blocklength (PPV) bound, $R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}$.</li>
          <li><strong>Dispersion is king:</strong> $V$ is the variance of the per-symbol information — AWGN $V=\tfrac{s(s+2)}{2(1+s)^2}(\log_2 e)^2$, BSC $V=p(1-p)\log_2^2\tfrac{1-p}{p}$, BEC $V=\epsilon_e(1-\epsilon_e)$ — and the $\sqrt{V/n}$ back-off is the whole story: halving $n$ multiplies the penalty by $\sqrt 2$.</li>
          <li><strong>The right figure of merit</strong> is the gap to the FBL bound in dB at a target BLER, not the (uncloseable) gap to capacity.</li>
          <li><strong>Minimum distance returns:</strong> a short code's few low-weight codewords make the union bound $\mathrm{BLER}\approx A_{d_{\min}}Q(\sqrt{2d_{\min}R\,E_b/N_0})$ predict a real error floor; maximise $d_{\min}$, minimise its multiplicity.</li>
          <li><strong>Girth is squeezed:</strong> the Moore/tree bound $n\ge 1+d_v(d_c-1)\sum[(d_v-1)(d_c-1)]^i$ forces $n$ up exponentially per girth rung (girth $6/8/10/12$ needs $n\ge 16/166/1666/16666$ for a $(3,6)$ code), so short codes must tolerate short cycles.</li>
          <li><strong>Trapping / absorbing sets</strong> are the small sub-graphs that stall BP and cause the floor — a single-packet disease from too little averaging.</li>
          <li><strong>Design levers:</strong> protograph base graph, quasi-cyclic circulant lifting ($n=Z\,n_b$), PEG/ACE to raise girth and kill absorbing sets, and 5G BG2 for short/low-rate blocks (rate $1/5$ to $2/3$, $K$ up to $3840$).</li>
          <li><strong>Decoding:</strong> layered min-sum BP as the workhorse, plus near-ML OSD (order $t$: $\sum_{i=0}^t\binom{k}{i}$ re-encodings) and GRAND, worthwhile because the BP-to-ML gap is larger and the combinatorics cheaper at short $n$.</li>
          <li><strong>Versus short polar:</strong> both hug the FBL bound with strong decoding; 5G uses polar (CA-SCL) for very-short control and LDPC (BG2) for data, including short blocks.</li>
        </ul>`
      },
      {
        h: String.raw`Further reading`,
        html: String.raw`<ul class="further-reading">
<li><a href="https://arxiv.org/abs/1812.08562" target="_blank" rel="noopener">Coskun, Durisi, Jerkovits, Liva, Ryan, Stein &amp; Steiner — "Efficient Error-Correcting Codes in the Short Blocklength Regime" (arXiv:1812.08562)</a> — the definitive survey of this topic: it frames the finite-blocklength benchmark, then compares short LDPC, short polar (CA-SCL), tail-biting convolutional and BCH-with-OSD codes against the same normal-approximation bound, and discusses OSD, GRAND-style near-ML decoding and the union-bound floor — the single best companion to every section here.</li>
<li><a href="https://arxiv.org/abs/1504.06526" target="_blank" rel="noopener">Durisi, Koch &amp; Popovski — "Towards Massive, Ultra-Reliable, and Low-Latency Wireless Communication with Short Packets" (Proc. IEEE, arXiv:1504.06526)</a> — the landmark survey of the short-packet regime: it explains why the finite-blocklength normal approximation $C-\sqrt{V/n}\,Q^{-1}(\epsilon)$ of Polyanskiy, Poor and Verd&uacute; (2010), not capacity, governs one-shot transmission, and ties the channel dispersion $V$ to URLLC and machine-type traffic — the framing behind the FBL, dispersion and single-packet sections.</li>
<li><a href="https://en.wikipedia.org/wiki/Low-density_parity-check_code" target="_blank" rel="noopener">Wikipedia — Low-density parity-check code</a> — a solid orientation on the LDPC family this topic specialises: the sparse parity-check matrix and Tanner graph, belief-propagation decoding, quasi-cyclic construction, the error floor and trapping sets, and the 5G NR base-graph-1 / base-graph-2 split — a cross-check on the construction and decoding sections without re-deriving the BP algebra.</li>
<li><a href="https://arxiv.org/abs/2405.07547" target="_blank" rel="noopener">Rowshan, Qiu, Xie, Gu &amp; Yuan — "Channel Coding Toward 6G: Technical Overview and Outlook" (arXiv:2405.07547)</a> — a broad survey of the coding schemes deployed in 5G and evolving toward 6G — LDPC, polar (and Reed-Muller), and turbo / convolutional codes — with their construction and decoding; the standards-level companion to the 5G LDPC (base graph 2) and short-LDPC-versus-short-polar sections.</li>
</ul>`
      }
    ],
    keyPoints: [
      String.raw`This is the <em>short-blocklength / single-packet</em> topic ($n\sim 32$ to $1000$ bits, one-shot, no HARQ combining, latency-critical). The general <em>LDPC Codes</em> topic owns the family — Tanner graph, sum-product / min-sum belief propagation, degree distributions, density-evolution <em>thresholds</em>, asymptotic capacity-approaching behaviour — which is cross-referenced here, not re-derived.`,
      String.raw`The single-packet metric is <strong>block error rate (BLER / FER)</strong>, not BER: URLLC ($\sim 1\,\mathrm{ms}$, $99.999\%$), IoT/mMTC short payloads and control signalling forbid retransmission and long interleaving, so one short codeword must succeed alone.`,
      String.raw`At short $n$ capacity $C$ and the BP threshold are the wrong benchmarks (they are $n\to\infty$ limits); the right one is the finite-blocklength (Polyanskiy-Poor-Verd&uacute;, 2010) bound, normal approximation $R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}$.`,
      String.raw`The $\sqrt{V/n}$ <strong>back-off</strong> is the whole story, set by the <em>channel dispersion</em> $V$ (variance of the per-symbol information). Halving $n$ multiplies the back-off by $\sqrt 2$.`,
      String.raw`Real AWGN: $C=\tfrac12\log_2(1+\mathrm{SNR})$, $V=\tfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}(\log_2 e)^2$ (at SNR $=1$: $C=0.5$, $V\approx 0.78$). BSC$(p)$: $V=p(1-p)\log_2^2\tfrac{1-p}{p}$. BEC$(\epsilon_e)$: $V=\epsilon_e(1-\epsilon_e)$.`,
      String.raw`Two channels can share a capacity but differ in dispersion; at $C=0.5$ the BEC ($V=0.25$) is easiest, AWGN ($V\approx 0.78$) middling, BSC ($V\approx 0.89$) hardest at short $n$.`,
      String.raw`The real figure of merit is the <strong>gap to the FBL bound in dB</strong> at a target BLER — not the gap to capacity, most of which is the uncloseable dispersion back-off.`,
      String.raw`For $R=0.5$, $\epsilon=10^{-5}$ on AWGN the FBL bound sits below capacity by about $3.4\,\mathrm{dB}$ at $n=128$ and $1.3\,\mathrm{dB}$ at $n=1000$ — physics, not code weakness.`,
      String.raw`Minimum distance matters again: a short code has few low-weight codewords, so the union bound $\mathrm{BLER}\approx A_{d_{\min}}Q(\sqrt{2\,d_{\min}R\,E_b/N_0})$ predicts a real <strong>error floor</strong>. Maximise $d_{\min}$ (slope), minimise its multiplicity $A_{d_{\min}}$ (height).`,
      String.raw`<strong>Girth is squeezed at short $n$:</strong> the Moore/tree bound $n\ge 1+d_v(d_c-1)\sum_{i=0}^{(g-6)/2}[(d_v-1)(d_c-1)]^i$ forces $n$ up exponentially per girth rung — for $(3,6)$, girth $6/8/10/12$ needs $n\ge 16/166/1666/16666$ — so short codes must tolerate length-4/6 cycles that correlate BP messages.`,
      String.raw`<strong>Trapping sets / near-codewords / absorbing sets</strong> are small sub-graphs where BP stalls and reinforces a wrong pattern; an $(a,b)$ set has $a$ wrong bits and $b$ unsatisfied checks, and absorbing sets (majority-satisfied) are the stable, floor-causing kind.`,
      String.raw`The waterfall is <em>shallower</em> at short $n$ (larger $\sqrt{V/n}$) and the gap between BP and true ML decoding is <em>larger</em> — so near-ML decoding is worth its cost here.`,
      String.raw`Design levers: <strong>protograph / multi-edge-type</strong> base graph; <strong>quasi-cyclic (QC)</strong> circulant lifting of a $m_b\times n_b$ base by $Z\times Z$ shifts giving $n=Z\,n_b$ (hardware-friendly); <strong>PEG</strong> and <strong>ACE</strong> to maximise girth and avoid small absorbing sets; puncturing/shortening plus a CRC for flexibility.`,
      String.raw`5G NR uses LDPC <strong>base graph 2 (BG2)</strong> for the short-block, low-rate regime (rate $1/5$ to $2/3$, $K$ up to $3840=10\cdot 384$), versus BG1 for long/high-rate ($1/3$ to $8/9$, $K$ up to $8448=22\cdot 384$); lift set $Z$ runs $2$ to $384$.`,
      String.raw`Decoding: layered min-sum BP (offset/normalised) is the workhorse; near-ML <strong>OSD order $t$</strong> costs $\sum_{i=0}^{t}\binom{k}{i}$ re-encodings (for $(128,64)$: $65$, $2081$, $43745$ at orders $1,2,3$), and <strong>GRAND</strong> guesses noise for very short codes; post-processing lowers the trapping-set floor.`,
      String.raw`Versus the competitor: both short LDPC and short polar (CA-SCL) hug the FBL bound with strong decoding; 5G chose <strong>polar</strong> for the very-short <em>control</em> channel (steep, no floor) and <strong>LDPC (BG2)</strong> for <em>data</em> even at short blocks (parallel throughput, flexibility).`
    ],
    diagram: [
      {
        title: String.raw`Finite-blocklength rate versus blocklength: the sqrt(V/n) gap to capacity`,
        svg: String.raw`<svg viewBox="0 0 540 300" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
<defs><linearGradient id="sldpc-gap" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#ffa94d" stop-opacity="0.28"/><stop offset="1" stop-color="#ffa94d" stop-opacity="0.05"/></linearGradient></defs>
<rect x="0" y="0" width="540" height="300" fill="#1c232e"/>
<text x="14" y="20" fill="#e6edf3" font-size="13">FBL max rate R*(n) climbs to capacity C as n grows (AWGN, SNR=1, BLER=1e-5)</text>
<text x="16" y="37" fill="#9aa7b5" font-size="10">the shaded gap is the dispersion back-off sqrt(V/n) Q^-1(eps): it shrinks like 1/sqrt(n)</text>
<!-- axes -->
<line x1="70" y1="77" x2="500" y2="77" stroke="#4dabf7" stroke-width="1.2" stroke-dasharray="6 3"/>
<text x="503" y="80" fill="#4dabf7" font-size="10">C = 0.5</text>
<line x1="70" y1="60" x2="70" y2="250" stroke="#9aa7b5" stroke-width="1"/>
<line x1="70" y1="250" x2="500" y2="250" stroke="#9aa7b5" stroke-width="1"/>
<text x="30" y="160" fill="#9aa7b5" font-size="9" transform="rotate(-90 30 160)">rate (bits/use)</text>
<text x="250" y="272" fill="#9aa7b5" font-size="9" text-anchor="middle">blocklength n  (log scale, 32 to 10000)</text>
<!-- shaded gap between R* curve and capacity -->
<polygon points="70,243 122,224 174,183 226,153 278,133 329,117 381,105 500,90 500,77 70,77" fill="url(#sldpc-gap)"/>
<!-- R* curve -->
<polyline points="70,243 122,224 174,183 226,153 278,133 329,117 381,105 500,90" fill="none" stroke="#63e6be" stroke-width="2.2"/>
<!-- marked points -->
<circle cx="174" cy="183" r="3.5" fill="#ff6b6b"/><text x="150" y="200" fill="#ff6b6b" font-size="9">n=128, R*=0.19</text>
<circle cx="329" cy="117" r="3.5" fill="#ffa94d"/><text x="305" y="110" fill="#ffa94d" font-size="9">n=1000, R*=0.39</text>
<!-- regime labels -->
<text x="120" y="63" fill="#ff6b6b" font-size="10" text-anchor="middle">short-packet regime</text>
<text x="120" y="75" fill="#9aa7b5" font-size="8" text-anchor="middle">(this topic)</text>
<text x="430" y="235" fill="#63e6be" font-size="9" text-anchor="middle">asymptotic regime</text>
<text x="430" y="247" fill="#9aa7b5" font-size="8" text-anchor="middle">(general LDPC topic)</text>
<!-- n tick labels -->
<text x="70" y="262" fill="#9aa7b5" font-size="8" text-anchor="middle">32</text>
<text x="174" y="262" fill="#9aa7b5" font-size="8" text-anchor="middle">128</text>
<text x="278" y="262" fill="#9aa7b5" font-size="8" text-anchor="middle">512</text>
<text x="329" y="262" fill="#9aa7b5" font-size="8" text-anchor="middle">1024</text>
<text x="500" y="262" fill="#9aa7b5" font-size="8" text-anchor="middle">10000</text>
<text x="16" y="290" fill="#63e6be" font-size="9">At n=128 the gap eats 60% of C; the code's job is to hug R*, not chase C.</text>
</svg>`,
        caption: String.raw`The signature short-packet picture. The dashed blue line is the AWGN capacity $C=0.5$ bits/use (at $\mathrm{SNR}=1$); the teal curve is the finite-blocklength maximum rate $R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}$ at $\epsilon=10^{-5}$, and the shaded orange band is the dispersion back-off $\sqrt{V/n}\,Q^{-1}(\epsilon)$ that separates them. At small $n$ (the short-packet regime this topic owns) the band is huge — at $n=128$ the achievable rate $R^*\approx 0.19$ is more than $60\%$ below capacity — and it closes only slowly, like $1/\sqrt n$, so even at $n=1000$ the code is still $\approx 23\%$ short of $C$. A short code cannot beat this band; its goal is to sit as close to the teal $R^*$ curve as possible, which is why the gap-to-FBL-bound, not the gap to capacity, is the figure of merit.`
      },
      {
        title: String.raw`Protograph to quasi-cyclic code: circulant lifting by Z`,
        svg: String.raw`<svg viewBox="0 0 540 300" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
<defs><marker id="sldpc-arr" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" fill="#9aa7b5"/></marker></defs>
<rect x="0" y="0" width="540" height="300" fill="#1c232e"/>
<text x="14" y="20" fill="#e6edf3" font-size="13">QC-LDPC construction: small base graph, then lift each edge to a ZxZ circulant</text>
<!-- LEFT: protograph base graph (2 checks x 4 vars shown as a mini example) -->
<text x="90" y="44" fill="#63e6be" font-size="10" text-anchor="middle">protograph (base) B</text>
<text x="90" y="57" fill="#9aa7b5" font-size="8" text-anchor="middle">m_b x n_b, shift labels s_ij</text>
<rect x="40" y="70" width="26" height="26" rx="5" fill="#1c232e" stroke="#63e6be"/><text x="53" y="88" fill="#e6edf3" font-size="10" text-anchor="middle">c1</text>
<rect x="114" y="70" width="26" height="26" rx="5" fill="#1c232e" stroke="#63e6be"/><text x="127" y="88" fill="#e6edf3" font-size="10" text-anchor="middle">c2</text>
<rect x="30" y="150" width="24" height="24" rx="5" fill="#1c232e" stroke="#4dabf7"/><text x="42" y="167" fill="#e6edf3" font-size="9" text-anchor="middle">v1</text>
<rect x="70" y="150" width="24" height="24" rx="5" fill="#1c232e" stroke="#4dabf7"/><text x="82" y="167" fill="#e6edf3" font-size="9" text-anchor="middle">v2</text>
<rect x="110" y="150" width="24" height="24" rx="5" fill="#1c232e" stroke="#4dabf7"/><text x="122" y="167" fill="#e6edf3" font-size="9" text-anchor="middle">v3</text>
<rect x="150" y="150" width="24" height="24" rx="5" fill="#1c232e" stroke="#4dabf7"/><text x="162" y="167" fill="#e6edf3" font-size="9" text-anchor="middle">v4</text>
<line x1="48" y1="150" x2="53" y2="96" stroke="#9aa7b5"/><text x="40" y="128" fill="#ffa94d" font-size="8">s=0</text>
<line x1="82" y1="150" x2="58" y2="96" stroke="#9aa7b5"/><text x="70" y="120" fill="#ffa94d" font-size="8">s=3</text>
<line x1="122" y1="150" x2="122" y2="96" stroke="#9aa7b5"/><text x="126" y="128" fill="#ffa94d" font-size="8">s=1</text>
<line x1="162" y1="150" x2="132" y2="96" stroke="#9aa7b5"/><text x="150" y="120" fill="#ffa94d" font-size="8">s=5</text>
<!-- arrow -->
<line x1="188" y1="150" x2="238" y2="150" stroke="#9aa7b5" stroke-width="1.4" marker-end="url(#sldpc-arr)"/>
<text x="213" y="140" fill="#b197fc" font-size="9" text-anchor="middle">lift x Z</text>
<text x="213" y="163" fill="#9aa7b5" font-size="8" text-anchor="middle">P^s = cyclic</text>
<text x="213" y="174" fill="#9aa7b5" font-size="8" text-anchor="middle">shift by s</text>
<!-- RIGHT: block circulant matrix -->
<text x="380" y="44" fill="#b197fc" font-size="10" text-anchor="middle">lifted H = array of ZxZ circulants</text>
<text x="380" y="57" fill="#9aa7b5" font-size="8" text-anchor="middle">n = Z n_b,  m = Z m_b</text>
<!-- 4x8 grid of blocks -->
<g stroke="#3a4652" stroke-width="1">
<rect x="270" y="70" width="220" height="120" fill="none" stroke="#9aa7b5"/>
</g>
<!-- fill some circulant blocks (P^s) and zeros -->
<g font-size="8" text-anchor="middle">
<rect x="270" y="70" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="297" y="89" fill="#b197fc">P^0</text>
<rect x="380" y="70" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="407" y="89" fill="#b197fc">P^3</text>
<rect x="325" y="70" width="55" height="30" fill="#1c232e"/><text x="352" y="89" fill="#3a4652">0</text>
<rect x="435" y="70" width="55" height="30" fill="#1c232e"/><text x="462" y="89" fill="#3a4652">0</text>
<rect x="325" y="100" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="352" y="119" fill="#b197fc">P^1</text>
<rect x="435" y="100" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="462" y="119" fill="#b197fc">P^5</text>
<rect x="270" y="100" width="55" height="30" fill="#1c232e"/><text x="297" y="119" fill="#3a4652">0</text>
<rect x="380" y="100" width="55" height="30" fill="#1c232e"/><text x="407" y="119" fill="#3a4652">0</text>
<rect x="270" y="130" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="297" y="149" fill="#b197fc">P^2</text>
<rect x="325" y="130" width="55" height="30" fill="#1c232e"/><text x="352" y="149" fill="#3a4652">0</text>
<rect x="380" y="130" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="407" y="149" fill="#b197fc">P^4</text>
<rect x="435" y="130" width="55" height="30" fill="#1c232e"/><text x="462" y="149" fill="#3a4652">0</text>
<rect x="325" y="160" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="352" y="179" fill="#b197fc">P^6</text>
<rect x="435" y="160" width="55" height="30" fill="#241a2e" stroke="#b197fc"/><text x="462" y="179" fill="#b197fc">P^0</text>
<rect x="270" y="160" width="55" height="30" fill="#1c232e"/><text x="297" y="179" fill="#3a4652">0</text>
<rect x="380" y="160" width="55" height="30" fill="#1c232e"/><text x="407" y="179" fill="#3a4652">0</text>
</g>
<!-- concrete numbers -->
<rect x="40" y="210" width="460" height="70" rx="6" fill="#111821" stroke="#3a4652"/>
<text x="52" y="230" fill="#e6edf3" font-size="10">Example: base 4 x 8, lift Z = 64  ->  n = 64 x 8 = 512, m = 64 x 4 = 256</text>
<text x="52" y="248" fill="#63e6be" font-size="10">k = n - m = 256,  rate R = 1 - m_b/n_b = 1 - 4/8 = 1/2</text>
<text x="52" y="266" fill="#9aa7b5" font-size="9">PEG / ACE choose the shifts s_ij to maximise girth and avoid small absorbing sets.</text>
</svg>`,
        caption: String.raw`Quasi-cyclic construction, the standard way to build a short LDPC code. On the left is the tiny <em>protograph</em> (base graph) with $m_b$ check and $n_b$ variable nodes and integer shift labels $s_{ij}$ on its edges. Each edge is then <em>lifted</em> by a factor $Z$: it becomes a $Z\times Z$ circulant permutation matrix $\mathbf P^{s}$ (a cyclic shift by $s$), and each absent edge becomes a $Z\times Z$ zero block, producing the block-circulant parity-check matrix on the right. The lift multiplies both dimensions by $Z$, so $n=Z\,n_b$, $m=Z\,m_b$, $k=n-m$, and the rate stays $R=1-m_b/n_b$. The worked case — a $4\times 8$ base lifted by $Z=64$ — gives a length-$512$, rate-$\tfrac12$ code with $k=256$. The circulant structure is what makes QC-LDPC hardware-friendly (process $Z$ bits in parallel), while PEG and ACE pick the shift values $s_{ij}$ to maximise girth and suppress the small absorbing sets that would otherwise floor a short code.`
      },
      {
        title: String.raw`Short-code BLER: shallow waterfall, error floor, and the FBL wall`,
        svg: String.raw`<svg viewBox="0 0 540 300" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif">
<rect x="0" y="0" width="540" height="300" fill="#1c232e"/>
<text x="14" y="20" fill="#e6edf3" font-size="13">BLER vs Eb/N0 for a short code: waterfall, floor, and the FBL bound</text>
<!-- axes -->
<line x1="70" y1="50" x2="70" y2="250" stroke="#9aa7b5" stroke-width="1"/>
<line x1="70" y1="250" x2="500" y2="250" stroke="#9aa7b5" stroke-width="1"/>
<text x="30" y="150" fill="#9aa7b5" font-size="9" transform="rotate(-90 30 150)">BLER (log)</text>
<text x="285" y="278" fill="#9aa7b5" font-size="9" text-anchor="middle">Eb/N0 (dB)</text>
<!-- y gridlines / decade labels -->
<g fill="#9aa7b5" font-size="8" text-anchor="end">
<line x1="70" y1="60" x2="500" y2="60" stroke="#232d3a"/><text x="64" y="63">1e0</text>
<line x1="70" y1="110" x2="500" y2="110" stroke="#232d3a"/><text x="64" y="113">1e-2</text>
<line x1="70" y1="160" x2="500" y2="160" stroke="#232d3a"/><text x="64" y="163">1e-4</text>
<line x1="70" y1="210" x2="500" y2="210" stroke="#232d3a"/><text x="64" y="213">1e-6</text>
<line x1="70" y1="250" x2="500" y2="250" stroke="#232d3a"/><text x="64" y="248">1e-8</text>
</g>
<!-- FBL bound (vertical wall) -->
<line x1="120" y1="50" x2="120" y2="250" stroke="#4dabf7" stroke-width="1.4" stroke-dasharray="5 3"/>
<text x="124" y="66" fill="#4dabf7" font-size="9">FBL bound</text>
<text x="124" y="77" fill="#9aa7b5" font-size="8">(best possible)</text>
<!-- BP curve: steep-ish then floors -->
<polyline points="150,70 185,95 220,130 255,168 290,192 330,200 380,203 460,205" fill="none" stroke="#ff6b6b" stroke-width="2.2"/>
<text x="385" y="197" fill="#ff6b6b" font-size="9">min-sum BP</text>
<text x="360" y="220" fill="#ff6b6b" font-size="8">error floor (trapping sets)</text>
<!-- OSD / near-ML curve: steeper, no floor -->
<polyline points="150,72 180,100 210,140 240,185 268,225 285,246" fill="none" stroke="#63e6be" stroke-width="2.2"/>
<text x="250" y="240" fill="#63e6be" font-size="9">OSD / near-ML</text>
<!-- short polar CA-SCL: steep, no floor -->
<polyline points="135,74 165,105 195,150 222,198 245,240" fill="none" stroke="#b197fc" stroke-width="2" stroke-dasharray="4 2"/>
<text x="150" y="255" fill="#b197fc" font-size="9">short polar (CA-SCL)</text>
<!-- annotations -->
<text x="300" y="120" fill="#9aa7b5" font-size="9">shallow waterfall</text>
<text x="300" y="132" fill="#9aa7b5" font-size="8">(short n: large sqrt(V/n))</text>
<!-- gap arrow between FBL and BP waterfall -->
<line x1="120" y1="150" x2="238" y2="150" stroke="#ffa94d" stroke-width="1" marker-end="url(#sldpc-arr2)"/>
<defs><marker id="sldpc-arr2" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" fill="#ffa94d"/></marker></defs>
<text x="150" y="145" fill="#ffa94d" font-size="8">gap to FBL (dB)</text>
<text x="16" y="292" fill="#63e6be" font-size="9">Near-ML (OSD) and CA-SCL polar hug the FBL wall with no floor; plain BP flattens on trapping sets.</text>
</svg>`,
        caption: String.raw`How a short code behaves, in the BLER-versus-$E_b/N_0$ plane. The dashed blue vertical line is the finite-blocklength bound — the leftmost $E_b/N_0$ at which <em>any</em> length-$n$ one-shot code could hit the target BLER, the wall of the first diagram seen from the SNR side. The red curve is plain min-sum belief propagation: its waterfall is <em>shallow</em> (short $n$ means a large $\sqrt{V/n}$, so the curve descends gently) and it <em>flattens into an error floor</em> once trapping / absorbing sets dominate. The teal curve is a near-ML decoder (ordered-statistics decoding): steeper, sitting only a fraction of a dB from the FBL wall, and with the floor pushed far down. The dashed purple curve is the short-polar competitor with CRC-aided SCL — also steep and floor-free, which is why 5G puts polar on the very-short control channel. The orange arrow is the true figure of merit: the horizontal gap in dB from the FBL bound to the operating code, not the distance to capacity.`
      }
    ],
    equations: [
      {
        title: 'Finite-blocklength normal approximation (PPV)',
        tex: String.raw`$$ R^*(n,\epsilon)\ \approx\ C\ -\ \sqrt{\frac{V}{n}}\;Q^{-1}(\epsilon)\ +\ \frac{\log_2 n}{2n} $$`,
        derivation: String.raw`<p><b>Where we start.</b> Shannon's capacity $C$ is the largest rate at which the error probability can be driven to zero <em>as $n\to\infty$</em>. It says nothing about a fixed short $n$ at a fixed non-zero block error probability $\epsilon$. We want the maximum rate $R^*(n,\epsilon)$ of the best length-$n$ code with block error $\le\epsilon$.</p>
<p><b>Step 1 — the exact second-order result.</b> Polyanskiy, Poor and Verd&uacute; (2010) proved that for a wide class of memoryless channels $R^*(n,\epsilon)=C-\sqrt{V/n}\,Q^{-1}(\epsilon)+O\!\left(\tfrac{\log n}{n}\right)$, where $V$ is the channel dispersion. Only two channel numbers appear: the mean of the per-symbol information (capacity $C$) and its variance (dispersion $V$).</p>
<p><b>Step 2 — where the middle term comes from.</b> Decoding hinges on the information density summed over $n$ symbols; it has mean $nC$ and standard deviation $\sqrt{nV}$ and is approximately Gaussian by the central limit theorem. Requiring that this sum exceed $nR$ with probability $1-\epsilon$ gives $nR\lesssim nC-\sqrt{nV}\,Q^{-1}(\epsilon)$, i.e. the per-use back-off $\sqrt{V/n}\,Q^{-1}(\epsilon)$. The $+\tfrac{\log_2 n}{2n}$ is the leading part of the third-order correction and is small.</p>
<p><b>Result.</b> $$ R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}. $$ This is the benchmark a short code must be measured against, not $C$. The back-off is positive for any $\epsilon<\tfrac12$ (so $Q^{-1}(\epsilon)>0$) and vanishes only as $n\to\infty$, at which point $R^*\to C$ and the asymptotic theory of the general LDPC topic takes over.</p>`
      },
      {
        title: 'AWGN capacity and channel dispersion',
        tex: String.raw`$$ C=\tfrac12\log_2\!\left(1+\mathrm{SNR}\right),\qquad V=\frac{\mathrm{SNR}\,(\mathrm{SNR}+2)}{2\,(1+\mathrm{SNR})^{2}}\,(\log_2 e)^{2} $$`,
        derivation: String.raw`<p><b>Where we start.</b> The dispersion $V$ is the variance of the per-symbol information density $i(X;Y)$, just as capacity is its mean. For the real AWGN channel $Y=X+N$ with signal power $P$, noise power $\sigma^2$ and $\mathrm{SNR}=P/\sigma^2$, we need both.</p>
<p><b>Step 1 — the mean (capacity).</b> The Gaussian-input mutual information per real channel use is $C=\tfrac12\log_2(1+\mathrm{SNR})$ bits — the familiar real-AWGN capacity from the <em>Shannon</em> topic.</p>
<p><b>Step 2 — the variance (dispersion).</b> Computing the variance of the information density under the capacity-achieving Gaussian input yields $V=\dfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}$ in nats$^2$; multiplying by $(\log_2 e)^2$ converts to bits$^2$ per channel use. Two limits check it: as $\mathrm{SNR}\to 0$, $V\to 0$ (a channel with no capacity has no dispersion); as $\mathrm{SNR}\to\infty$, $V\to\tfrac12(\log_2 e)^2\approx 1.04$, a finite ceiling.</p>
<p><b>Result.</b> At $\mathrm{SNR}=1$: $C=\tfrac12\log_2 2=0.5$ bits/use and $V=\tfrac{1\cdot 3}{2\cdot 4}(\log_2 e)^2=0.375\cdot 2.081=0.781$ bits$^2$/use. These are the two numbers that feed the normal approximation; the whole short-block penalty $\sqrt{V/n}\,Q^{-1}(\epsilon)$ is built from this $V$, which is why dispersion — not capacity — is the quantity a short-packet designer tracks.</p>`
      },
      {
        title: 'The square-root back-off and its scaling with blocklength',
        tex: String.raw`$$ \Delta R(n)=\sqrt{\frac{V}{n}}\;Q^{-1}(\epsilon),\qquad \frac{\Delta R(n/2)}{\Delta R(n)}=\sqrt{2} $$`,
        derivation: String.raw`<p><b>Where we start.</b> The single term that separates the finite-blocklength rate from capacity is the back-off $\Delta R(n)=C-R^*\approx\sqrt{V/n}\,Q^{-1}(\epsilon)$ (ignoring the small third-order term). We want to know how it scales when we shorten the packet.</p>
<p><b>Step 1 — isolate the $n$ dependence.</b> At fixed channel ($V$) and fixed reliability ($\epsilon$, hence fixed $Q^{-1}(\epsilon)$), the only variable is $n$, and $\Delta R(n)\propto n^{-1/2}$. Halving the blocklength replaces $n$ by $n/2$.</p>
<p><b>Step 2 — take the ratio.</b> $\dfrac{\Delta R(n/2)}{\Delta R(n)}=\dfrac{\sqrt{V/(n/2)}}{\sqrt{V/n}}=\sqrt{\dfrac{n}{n/2}}=\sqrt{2}\approx 1.414$. Numerically, for AWGN at $\mathrm{SNR}=1$ ($V=0.781$) and $\epsilon=10^{-5}$ ($Q^{-1}=4.265$): at $n=128$, $\Delta R=\sqrt{0.781/128}\cdot 4.265=0.333$ bits; at $n=64$, $\Delta R=\sqrt{0.781/64}\cdot 4.265=0.471$ bits, and $0.471/0.333=1.414=\sqrt2$.</p>
<p><b>Result.</b> Every halving of the blocklength multiplies the rate penalty by $\sqrt 2\approx 41\%$, and every doubling divides it by $\sqrt 2$. The back-off is <em>relentless but slow</em> — it never disappears at finite $n$, yet it only shrinks as $1/\sqrt n$, so buying back a factor-of-two in penalty costs a factor-of-four in blocklength (and hence latency). This trade — reliability and rate against latency — is the central tension of the short-packet regime.</p>`
      },
      {
        title: 'Union-bound block error and the error floor',
        tex: String.raw`$$ \mathrm{BLER}\ \le\ \sum_{w\ge d_{\min}} A_w\,Q\!\left(\sqrt{2wR\,\tfrac{E_b}{N_0}}\right)\ \approx\ A_{d_{\min}}\,Q\!\left(\sqrt{2\,d_{\min}R\,\tfrac{E_b}{N_0}}\right) $$`,
        derivation: String.raw`<p><b>Where we start.</b> At short $n$ a code has only a small, countable set of low-weight codewords, and they dominate the high-SNR error rate. We bound the maximum-likelihood block error probability from the weight enumerator $\{A_w\}$ (the number of codewords of Hamming weight $w$).</p>
<p><b>Step 1 — pairwise error, then union.</b> On the AWGN/BPSK channel the probability of confusing the transmitted codeword with a specific weight-$w$ codeword is $Q(\sqrt{2wRE_b/N_0})$ (the two differ in $w$ coordinates, and $E_s=RE_b$). The union bound sums the pairwise terms over all non-zero codewords: $\mathrm{BLER}\le\sum_{w\ge d_{\min}}A_w\,Q(\sqrt{2wRE_b/N_0})$.</p>
<p><b>Step 2 — the dominant term.</b> Because $Q$ falls faster than exponentially, the smallest-weight term dominates at high SNR: $\mathrm{BLER}\approx A_{d_{\min}}Q(\sqrt{2\,d_{\min}RE_b/N_0})$. The minimum distance $d_{\min}$ sets the <em>slope</em> (larger $d_{\min}$ steepens the waterfall via $\sqrt{d_{\min}}$), and the multiplicity $A_{d_{\min}}$ sets the <em>height</em>.</p>
<p><b>Result.</b> For $d_{\min}=10$, $A_{d_{\min}}=30$, $R=\tfrac12$ at $E_b/N_0=4\,\mathrm{dB}$: the argument is $\sqrt{2\cdot 10\cdot 0.5\cdot 10^{0.4}}=\sqrt{25.12}=5.01$, $Q(5.01)=2.7\times10^{-7}$, so $\mathrm{BLER}\approx 30\cdot 2.7\times10^{-7}=8.1\times10^{-6}$. This flattening onto the union-bound line is the <em>error floor</em> — invisible at long $n$ (dense spectrum, floor far below any BLER of interest) but a first-order short-code concern, which is why maximising $d_{\min}$ and minimising its multiplicity matters here.</p>`
      },
      {
        title: 'Moore / tree bound: girth costs blocklength',
        tex: String.raw`$$ n\ \ge\ 1+d_v(d_c-1)\sum_{i=0}^{(g-6)/2}\big[(d_v-1)(d_c-1)\big]^{i} $$`,
        derivation: String.raw`<p><b>Where we start.</b> Belief propagation is exact only on a tree; the girth $g$ (shortest cycle, even, $\ge 4$) controls how far from a tree the Tanner graph is. We ask the reverse question: given degrees $(d_v,d_c)$, how many variable nodes $n$ are <em>needed</em> to even permit a girth of $g$?</p>
<p><b>Step 1 — expand a tree.</b> Root at a variable node (level 0: $1$ node). It has $d_v$ checks, each joined to $d_c-1$ other variable nodes, giving $d_v(d_c-1)$ variable nodes at the next level. Each of those has $d_v-1$ new checks, each with $d_c-1$ new variable nodes, multiplying by $(d_v-1)(d_c-1)$ per further level.</p>
<p><b>Step 2 — forbid early cycle closure.</b> For the girth to be at least $g$, all variable nodes encountered up to depth $(g-2)/2$ must be <em>distinct</em> (a repeat is a cycle shorter than $g$). Summing the distinct nodes gives $n\ge 1+d_v(d_c-1)\sum_{i=0}^{(g-6)/2}[(d_v-1)(d_c-1)]^i$; a coincidence among the first $d_v(d_c-1)$ nodes is exactly a length-4 cycle, so girth $\ge 6$ already needs $n\ge 1+d_v(d_c-1)$.</p>
<p><b>Result.</b> For the rate-$\tfrac12$ $(d_v,d_c)=(3,6)$ ensemble: girth $\ge 6$ needs $n\ge 1+3\cdot5=16$; girth $\ge 8$ needs $n\ge 1+15(1+10)=166$; girth $\ge 10$ needs $n\ge 1+15(1+10+100)=1666$; girth $\ge 12$ needs $16666$. Each extra rung multiplies the required $n$ by about $(d_v-1)(d_c-1)=10$ — <em>exponential</em> growth. A short code physically cannot achieve large girth, so it is forced to contain the very short cycles that degrade BP; this combinatorial squeeze is unique to small $n$.</p>`
      },
      {
        title: 'Quasi-cyclic circulant lifting',
        tex: String.raw`$$ \mathbf H=\big[\,\mathbf P^{\,s_{ij}}\,\big]_{i\le m_b,\,j\le n_b},\qquad n=Z\,n_b,\quad m=Z\,m_b,\quad R=1-\frac{m_b}{n_b} $$`,
        derivation: String.raw`<p><b>Where we start.</b> We want a short LDPC code that is structured enough for fast hardware yet flexible enough to tune girth. The quasi-cyclic construction lifts a tiny base graph by structured permutations.</p>
<p><b>Step 1 — the base graph.</b> Fix a protograph: an $m_b\times n_b$ base matrix whose entries are either "no edge" or a small integer shift $s_{ij}\in\{0,\dots,Z-1\}$. It fixes the degree profile and, via protograph density evolution, the asymptotic behaviour, while being small enough to reason about.</p>
<p><b>Step 2 — lift by $Z$.</b> Replace each shift entry $s_{ij}$ by the $Z\times Z$ circulant permutation matrix $\mathbf P^{s_{ij}}$ (identity cyclically shifted by $s_{ij}$) and each "no edge" by the $Z\times Z$ zero matrix. The result $\mathbf H$ is an $m_bZ\times n_bZ$ block-circulant parity-check matrix, so $m=Z\,m_b$, $n=Z\,n_b$, $k=n-m$, and the rate is unchanged at $R=1-m_b/n_b$.</p>
<p><b>Result.</b> A $4\times 8$ base ($m_b=4,n_b=8$) lifted by $Z=64$ gives $n=512$, $m=256$, $k=256$, $R=1-4/8=\tfrac12$. The circulant blocks let a decoder process $Z$ bits in parallel and store the code as a handful of shift integers, which is why quasi-cyclic LDPC is the dominant short-code structure in standards (including 5G BG1/BG2). PEG and ACE then choose the shifts $s_{ij}$ to maximise girth and avoid small absorbing sets.</p>`
      },
      {
        title: 'Ordered-statistics decoding: order-t complexity',
        tex: String.raw`$$ N_{\text{TEP}}(t)=\sum_{i=0}^{t}\binom{k}{i} $$`,
        derivation: String.raw`<p><b>Where we start.</b> At short $n$ belief propagation leaves a gap to maximum-likelihood decoding. Ordered-statistics decoding (OSD) closes most of it by reprocessing the most reliable positions, and we count its cost.</p>
<p><b>Step 1 — the reprocessing idea.</b> Sort the $n$ received positions by reliability; take the $k$ most reliable independent positions as a temporary information set and re-encode to get a first codeword estimate. Then generate <em>test error patterns</em> (TEPs): flip up to $t$ of those $k$ reliable positions, re-encode each flipped pattern, and keep whichever resulting codeword is closest to the received word.</p>
<p><b>Step 2 — count the patterns.</b> Order-$t$ OSD tries every error pattern of weight $0,1,\dots,t$ over the $k$ positions, so the number of re-encodings is $N_{\text{TEP}}(t)=\sum_{i=0}^{t}\binom{k}{i}$. Order $0$ is the single re-encode ($1$); order $1$ adds $\binom{k}{1}=k$; order $2$ adds $\binom{k}{2}$; and so on.</p>
<p><b>Result.</b> For a $(128,64)$ code, $k=64$: order $1$ costs $1+64=65$; order $2$ costs $1+64+2016=2081$; order $3$ costs $1+64+2016+41664=43745$. Complexity grows polynomially in $k$ per order ($\binom{k}{t}\sim k^t$), which is affordable precisely because $k$ is <em>small</em> in the short regime — one reason near-ML decoding is practical for short codes but not long ones. Even order $2$ typically gets a short code within a small fraction of a dB of ML, hence within reach of the FBL bound.</p>`
      },
      {
        title: 'Dispersion of the BSC and BEC',
        tex: String.raw`$$ \text{BSC}(p):\ V=p(1-p)\log_2^{2}\!\frac{1-p}{p};\qquad \text{BEC}(\epsilon_e):\ V=\epsilon_e(1-\epsilon_e) $$`,
        derivation: String.raw`<p><b>Where we start.</b> The AWGN dispersion has a messy closed form; the two canonical binary channels have clean ones, and they make the "dispersion, not capacity, decides short-block pain" point vivid. Both are variances of the per-symbol information density.</p>
<p><b>Step 1 — BSC$(p)$.</b> The information density takes two values depending on whether the bit flipped, with probabilities $1-p$ and $p$; its mean is $C=1-H_2(p)$ and its variance works out to $V=p(1-p)\log_2^2\tfrac{1-p}{p}$ bits$^2$/use. For $p=0.11$: $C\approx 0.500$ and $V=0.11\cdot0.89\cdot(\log_2(0.89/0.11))^2\approx 0.891$.</p>
<p><b>Step 2 — BEC$(\epsilon_e)$.</b> Here the information density is a clean Bernoulli: exactly $1$ bit when the symbol survives (probability $1-\epsilon_e$), exactly $0$ when erased (probability $\epsilon_e$). Its mean is $C=1-\epsilon_e$ and its variance is that of a scaled Bernoulli, $V=\epsilon_e(1-\epsilon_e)$ bits$^2$/use. For $\epsilon_e=0.5$: $C=0.5$, $V=0.25$.</p>
<p><b>Result.</b> Compare the three channels at the <em>same</em> capacity $C=0.5$: BEC $V=0.25$, AWGN $V\approx 0.78$, BSC $V\approx 0.89$. Equal capacity, unequal dispersion — so equal-capacity channels impose <em>different</em> short-block back-offs, with the BEC easiest and the BSC hardest. This is the cleanest demonstration that the finite-blocklength penalty is a second-order (variance) effect that capacity alone cannot predict.</p>`
      }
    ],
    flashcards: [
      { front: String.raw`How does this topic divide labour with the general LDPC topic?`, back: String.raw`This topic owns the <em>short-blocklength, single-packet</em> regime ($n\sim 32$-$1000$, one-shot, no HARQ) where asymptotics fail: FBL bound, dispersion, min-distance floors, girth/Moore bound, trapping sets, QC construction, near-ML decoding. The general <em>LDPC</em> topic owns the family — Tanner graph, sum-product/min-sum BP, degree distributions, density-evolution thresholds — cross-referenced here, not re-derived.` },
      { front: String.raw`What defines the single-packet setting, and what is its metric?`, back: String.raw`One short codeword decoded one-shot with no retransmission and no HARQ chase-combining / incremental redundancy across packets, under a latency budget (URLLC, IoT/mMTC, control). Metric: <strong>block error rate (BLER/FER)</strong>, not BER — the packet is right or wrong as a unit.` },
      { front: String.raw`Why do capacity and the BP threshold mislead at short $n$?`, back: String.raw`Both are $n\to\infty$ limits: capacity is the mean per-symbol information and the density-evolution threshold assumes an infinitely long codeword. At short $n$ fluctuations do not average out, so the right benchmark is the finite-blocklength (PPV) bound, which depends on both $n$ and $\epsilon$.` },
      { front: String.raw`State the finite-blocklength normal approximation.`, back: String.raw`$R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}$: capacity minus a back-off that scales like $1/\sqrt n$, set by the channel dispersion $V$, plus a small third-order term.` },
      { front: String.raw`What is the channel dispersion $V$?`, back: String.raw`The variance (per channel use) of the information density — the second-order statistic to capacity's mean. It alone sets the size of the $\sqrt{V/n}$ short-block back-off; equal-capacity channels with different $V$ suffer differently.` },
      { front: String.raw`Give $C$ and $V$ for the real AWGN channel.`, back: String.raw`$C=\tfrac12\log_2(1+\mathrm{SNR})$; $V=\tfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}(\log_2 e)^2$. At SNR $=1$: $C=0.5$ bits/use, $V\approx 0.78$ bits$^2$/use.` },
      { front: String.raw`Give $V$ for the BSC and BEC.`, back: String.raw`BSC$(p)$: $C=1-H_2(p)$, $V=p(1-p)\log_2^2\tfrac{1-p}{p}$ ($p=0.11\Rightarrow V\approx 0.89$). BEC$(\epsilon_e)$: $C=1-\epsilon_e$, $V=\epsilon_e(1-\epsilon_e)$ ($\epsilon_e=0.5\Rightarrow V=0.25$).` },
      { front: String.raw`How does the back-off scale when $n$ is halved?`, back: String.raw`$\Delta R\propto n^{-1/2}$, so halving $n$ multiplies the penalty by $\sqrt 2\approx 1.41$; buying back a $\sqrt2$ in penalty costs a factor of $4$ in blocklength (and latency).` },
      { front: String.raw`What is the real figure of merit for a short code?`, back: String.raw`The gap to the <strong>FBL bound</strong> in dB at a target BLER — <em>not</em> the gap to capacity, most of which is the uncloseable dispersion back-off. A good short code sits a fraction of a dB from the FBL bound.` },
      { front: String.raw`Why does minimum distance matter again at short $n$?`, back: String.raw`A short code has few low-weight codewords, so the union bound $\mathrm{BLER}\approx A_{d_{\min}}Q(\sqrt{2d_{\min}RE_b/N_0})$ is visible: $d_{\min}$ sets the waterfall slope, its multiplicity $A_{d_{\min}}$ sets the height, and together they produce an <strong>error floor</strong>.` },
      { front: String.raw`Why can't a short code have large girth?`, back: String.raw`The Moore/tree bound $n\ge 1+d_v(d_c-1)\sum[(d_v-1)(d_c-1)]^i$ makes the blocklength grow exponentially per girth rung — for $(3,6)$, girth $6/8/10/12$ needs $n\ge 16/166/1666/16666$ — so a short code is forced to contain short cycles that correlate BP messages.` },
      { front: String.raw`What is a trapping / absorbing set?`, back: String.raw`A small sub-graph of variable nodes that, when in error, leaves too few unsatisfied checks to correct them, so BP stalls. An $(a,b)$ set has $a$ wrong bits and $b$ unsatisfied checks; an <em>absorbing set</em> (each wrong bit has more satisfied than unsatisfied checks) is the stable, floor-causing kind.` },
      { front: String.raw`Why is the error floor a single-packet disease?`, back: String.raw`A trapping set fires when noise lands on its few specific bits in one transmission; with no averaging across retransmissions and no long-block dilution, that surfaces at $10^{-5}$-$10^{-9}$ BLER — right in the URLLC operating range. At long $n$ the same sets sit far below any BLER of interest.` },
      { front: String.raw`What are the main short-LDPC design levers?`, back: String.raw`Protograph / multi-edge-type base graph; quasi-cyclic circulant lifting ($n=Z\,n_b$, hardware-friendly); PEG and ACE to maximise girth and avoid small absorbing sets; puncturing/shortening plus a CRC for rate/length flexibility.` },
      { front: String.raw`What is 5G LDPC base graph 2 (BG2) for?`, back: String.raw`The short-block, low-rate regime: base $42\times 52$, $10$ info columns, rate $1/5$ to $2/3$, max $K=3840$. BG1 ($46\times 68$, $22$ info columns, rate $1/3$ to $8/9$, $K$ up to $8448$) serves long/high-rate blocks. Lift set $Z$: $2$ to $384$.` },
      { front: String.raw`How does ordered-statistics decoding (OSD) work and what does it cost?`, back: String.raw`Re-encode from the $k$ most reliable positions, then try all error patterns of weight $\le t$ over them, keeping the closest codeword. Cost $=\sum_{i=0}^{t}\binom{k}{i}$ re-encodings; for $(128,64)$ that is $65$ (order 1), $2081$ (order 2), $43745$ (order 3). Near-ML, and affordable because $k$ is small.` },
      { front: String.raw`What is GRAND, and where does it fit?`, back: String.raw`Guessing Random Additive Noise Decoding: guess the <em>noise</em> patterns in likelihood order, subtract from the received word, stop when the result is a codeword. Near-ML for any short code (needs only a membership test), efficient for very short, high-rate codes where few noise patterns are likely.` },
      { front: String.raw`Short LDPC vs short polar (CA-SCL): the honest summary?`, back: String.raw`Both hug the FBL bound with strong decoding. Polar+CA-SCL is steep with essentially no floor (5G's choice for very-short <em>control</em>); LDPC BG2 has a parallel low-latency decoder and native flexibility but possible trapping-set floors (5G's choice for <em>data</em>, including short blocks).` }
    ],
    mcqs: [
      { q: String.raw`Compared with the general LDPC topic, this topic focuses on:`, options: [String.raw`the sum-product / min-sum message-update algebra`, String.raw`density-evolution thresholds as $n\to\infty$`, String.raw`the short-blocklength, single-packet regime where asymptotics break down`, String.raw`the Tanner-graph incidence rule and girth definitions`], answer: 2, explain: String.raw`This topic owns the short, one-shot regime (FBL bound, dispersion, floors, near-ML). The BP algebra, thresholds and Tanner-graph definitions belong to the <em>LDPC</em> and <em>Tanner Graph</em> topics and are cross-referenced, not re-derived.` },
      { q: String.raw`The performance metric for a single short packet is:`, options: [String.raw`block / frame error rate (BLER / FER)`, String.raw`bit error rate (BER)`, String.raw`symbol error rate after HARQ combining`, String.raw`the density-evolution threshold`], answer: 0, explain: String.raw`One codeword is right or wrong as a unit; a single surviving bit error ruins the packet, so BLER/FER is the metric, and there is no HARQ combining across packets.` },
      { q: String.raw`Why are capacity and the BP threshold poor benchmarks at short $n$?`, options: [String.raw`they are pessimistic at short $n$`, String.raw`they are $n\to\infty$ limits, so they ignore the finite-length back-off`, String.raw`they depend on the decoder used`, String.raw`they only apply to the BEC`], answer: 1, explain: String.raw`Both describe infinitely long codewords; at short $n$ the relevant benchmark is the finite-blocklength bound, which depends on $n$ and $\epsilon$.` },
      { q: String.raw`In $R^*(n,\epsilon)\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}$, the quantity $V$ is:`, options: [String.raw`the code rate`, String.raw`the minimum distance`, String.raw`the channel dispersion (variance of the information density)`, String.raw`the number of BP iterations`], answer: 2, explain: String.raw`$V$ is the dispersion — the variance of the per-symbol information density, the second-order statistic to capacity's mean — and it sizes the $\sqrt{V/n}$ back-off.` },
      { q: String.raw`For the real AWGN channel the dispersion is:`, options: [String.raw`$V=\tfrac12\log_2(1+\mathrm{SNR})$`, String.raw`$V=\tfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}(\log_2 e)^2$`, String.raw`$V=p(1-p)\log_2^2\tfrac{1-p}{p}$`, String.raw`$V=\epsilon_e(1-\epsilon_e)$`], answer: 1, explain: String.raw`Option 0 is the AWGN capacity; option 2 is the BSC dispersion; option 3 is the BEC dispersion. The AWGN dispersion is $\tfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}(\log_2 e)^2$.` },
      { q: String.raw`Halving the blocklength $n$ multiplies the rate back-off by:`, options: [String.raw`$2$`, String.raw`$1/2$`, String.raw`$\sqrt 2$`, String.raw`it is unchanged`], answer: 2, explain: String.raw`$\Delta R\propto n^{-1/2}$, so replacing $n$ by $n/2$ multiplies the penalty by $\sqrt{n/(n/2)}=\sqrt 2\approx 1.41$.` },
      { q: String.raw`The correct figure of merit for a short code is the gap to:`, options: [String.raw`the Shannon capacity, in dB`, String.raw`the finite-blocklength (FBL) bound, in dB at a target BLER`, String.raw`the BP threshold, in dB`, String.raw`the uncoded curve, in dB`], answer: 1, explain: String.raw`Most of the gap to capacity is the uncloseable dispersion back-off; the gap to the FBL bound isolates the code's own quality.` },
      { q: String.raw`At short $n$ the union bound $\mathrm{BLER}\approx A_{d_{\min}}Q(\sqrt{2d_{\min}RE_b/N_0})$ is important because:`, options: [String.raw`long codes have no minimum distance`, String.raw`it replaces belief propagation`, String.raw`it only holds on the BEC`, String.raw`a short code has few low-weight codewords, so they dominate and create an error floor`], answer: 3, explain: String.raw`A short code's countable low-weight codewords sit in the operating range; $d_{\min}$ sets the slope and its multiplicity the height of the resulting floor.` },
      { q: String.raw`For a $(3,6)$-regular code, the Moore/tree bound says girth $\ge 8$ requires at least:`, options: [String.raw`$8$ variable nodes`, String.raw`$16$ variable nodes`, String.raw`$166$ variable nodes`, String.raw`$1666$ variable nodes`], answer: 2, explain: String.raw`$n\ge 1+3\cdot5(1+2\cdot5)=1+15\cdot 11=166$. Girth $6$ needs $16$; girth $10$ needs $1666$ — each rung multiplies $n$ by about $10$.` },
      { q: String.raw`An absorbing set is dangerous because:`, options: [String.raw`it increases the code rate`, String.raw`each erroneous bit has more satisfied than unsatisfied checks, so BP stabilises the error`, String.raw`it removes all short cycles`, String.raw`it only occurs at long blocklengths`], answer: 1, explain: String.raw`In an absorbing set the local majority of checks is satisfied, so belief propagation reinforces rather than corrects the error — the stable, floor-causing case of a trapping set.` },
      { q: String.raw`In quasi-cyclic lifting, a base graph of size $m_b\times n_b$ lifted by $Z$ yields a code of length:`, options: [String.raw`$n=Z\,n_b$`, String.raw`$n=Z+n_b$`, String.raw`$n=Z^{n_b}$`, String.raw`$n=n_b/Z$`], answer: 0, explain: String.raw`Each edge becomes a $Z\times Z$ circulant, so both dimensions scale by $Z$: $n=Z\,n_b$, $m=Z\,m_b$, and the rate stays $1-m_b/n_b$.` },
      { q: String.raw`5G NR LDPC base graph 2 (BG2) is designed for:`, options: [String.raw`long, high-rate data blocks`, String.raw`control channels only`, String.raw`analog transmission`, String.raw`short, low-rate blocks`], answer: 3, explain: String.raw`BG2 ($42\times 52$, $10$ info columns, rate $1/5$ to $2/3$, $K$ up to $3840$) targets short/low-rate blocks; BG1 handles long/high-rate ($1/3$ to $8/9$).` },
      { q: String.raw`Ordered-statistics decoding of order $t$ on a code with $k$ information bits performs how many re-encodings?`, options: [String.raw`$\binom{k}{t}$`, String.raw`$k^t$`, String.raw`$\sum_{i=0}^{t}\binom{k}{i}$`, String.raw`$2^k$`], answer: 2, explain: String.raw`OSD tests all error patterns of weight $0,1,\dots,t$ over the $k$ reliable positions, so the count is $\sum_{i=0}^{t}\binom{k}{i}$ — e.g. $2081$ for $(128,64)$ at order $2$.` },
      { q: String.raw`Why is near-ML decoding (OSD/GRAND) more attractive at short $n$ than long $n$?`, options: [String.raw`the BP-to-ML gap is larger and the combinatorial cost is smaller at short $n$`, String.raw`long codes have no maximum-likelihood decoder`, String.raw`short codes have no belief-propagation decoder`, String.raw`it lowers the code rate`], answer: 0, explain: String.raw`At short $n$ forced short cycles/trapping sets widen the BP-to-ML gap (more to gain) while small $k,n$ keep OSD/GRAND cheap (less to pay) — the opposite balance from long codes.` },
      { q: String.raw`In 5G NR, the short-code split between polar and LDPC is:`, options: [String.raw`polar for very-short control, LDPC (BG2) for data including short blocks`, String.raw`polar for data, LDPC for control`, String.raw`both used only for control`, String.raw`LDPC for everything short`], answer: 0, explain: String.raw`5G uses CRC-aided SCL polar on the very-short control channels (steep, no floor) and LDPC with base graph 2 on the data channels even at short block lengths.` },
      { q: String.raw`Compared with a long code, a short LDPC code's waterfall is:`, options: [String.raw`steeper, because $\sqrt{V/n}$ is smaller`, String.raw`identical, since the channel is the same`, String.raw`vertical at capacity`, String.raw`shallower, because $\sqrt{V/n}$ is larger`], answer: 3, explain: String.raw`The back-off $\sqrt{V/n}$ grows as $n$ shrinks, so the achievable-rate curve — and the BLER waterfall — is shallower at short $n$.` }
    ],
    numericals: [
      { q: String.raw`AWGN at $\mathrm{SNR}=1$ ($0\,\mathrm{dB}$), $\epsilon=10^{-5}$. Find the capacity $C$, dispersion $V$, the back-off at $n=128$, the FBL max rate $R^*$, and the gap to capacity as a fraction of $C$.`, solution: String.raw`<p><b>Formula.</b> $$ C=\tfrac12\log_2(1+\mathrm{SNR}),\quad V=\tfrac{\mathrm{SNR}(\mathrm{SNR}+2)}{2(1+\mathrm{SNR})^2}(\log_2 e)^2,\quad R^*\approx C-\sqrt{V/n}\,Q^{-1}(\epsilon)+\tfrac{\log_2 n}{2n}. $$</p>
<p><b>Substitute.</b> $\mathrm{SNR}=1$: $C=\tfrac12\log_2 2$; $V=\tfrac{1\cdot3}{2\cdot4}(1.4427)^2$; $Q^{-1}(10^{-5})=4.265$; $n=128$, $\log_2 128=7$.</p>
<p><b>Compute.</b> $C=0.5$ bits/use; $V=0.375\cdot2.081=0.781$; back-off $=\sqrt{0.781/128}\cdot4.265=0.0781\cdot4.265=0.333$ bits; third term $=7/256=0.0273$; $R^*=0.5-0.333+0.027=0.194$ bits/use; gap $=0.5-0.194=0.306$ bits $=61\%$ of $C$.</p>
<p><b>Explanation.</b> At only $128$ bits and five-nines reliability the achievable rate is under $40\%$ of capacity — the dispersion back-off eats the majority of $C$. Sanity check: the back-off term $\sqrt{V/n}\,Q^{-1}$ dwarfs the small $+0.027$ correction, confirming that the middle term is the whole story at short $n$.</p>` },
      { q: String.raw`Same channel ($V=0.781$, $\epsilon=10^{-5}$). Compare the back-off at $n=128$ and $n=64$ and verify the halving rule.`, solution: String.raw`<p><b>Formula.</b> $$ \Delta R(n)=\sqrt{V/n}\,Q^{-1}(\epsilon),\qquad \frac{\Delta R(n/2)}{\Delta R(n)}=\sqrt2. $$</p>
<p><b>Substitute.</b> $Q^{-1}(10^{-5})=4.265$. $\Delta R(128)=\sqrt{0.781/128}\cdot4.265$; $\Delta R(64)=\sqrt{0.781/64}\cdot4.265$.</p>
<p><b>Compute.</b> $\sqrt{0.781/128}=0.0781\Rightarrow\Delta R(128)=0.333$ bits; $\sqrt{0.781/64}=0.1104\Rightarrow\Delta R(64)=0.471$ bits; ratio $=0.471/0.333=1.414=\sqrt2$.</p>
<p><b>Explanation.</b> Halving the block from $128$ to $64$ bits inflates the rate penalty by $41\%$, exactly $\sqrt2$. Sanity check: the ratio is independent of $V$ and $\epsilon$ (they cancel), so it is a pure $n^{-1/2}$ scaling law — buying back that $\sqrt2$ would cost a factor of $4$ in blocklength and hence latency.</p>` },
      { q: String.raw`Build a QC-LDPC code from a $4\times 8$ protograph with lift $Z=64$. Give $n$, $m$, $k$ and the rate $R$.`, solution: String.raw`<p><b>Formula.</b> $$ n=Z\,n_b,\quad m=Z\,m_b,\quad k=n-m,\quad R=1-\frac{m_b}{n_b}. $$</p>
<p><b>Substitute.</b> $m_b=4$, $n_b=8$, $Z=64$: $n=64\cdot8$; $m=64\cdot4$; $k=n-m$; $R=1-4/8$.</p>
<p><b>Compute.</b> $n=512$; $m=256$; $k=512-256=256$; $R=1-0.5=0.5$.</p>
<p><b>Explanation.</b> The lift multiplies both dimensions by $Z$ but leaves the rate fixed at the base-graph value $1-m_b/n_b=\tfrac12$. Sanity check: $k=n-m=256$ information bits over $n=512$ coded bits gives $R=k/n=0.5$, consistent with $1-m_b/n_b$ — and the code is stored as just the $4\times 8$ shift integers, the hardware advantage of quasi-cyclic structure.</p>` },
      { q: String.raw`For a rate-$\tfrac12$ $(d_v,d_c)=(3,6)$ code, use the Moore/tree bound to find the minimum blocklength needed for girth $6$ and girth $8$, and comment on a $128$-bit code.`, solution: String.raw`<p><b>Formula.</b> $$ n\ge 1+d_v(d_c-1)\sum_{i=0}^{(g-6)/2}\big[(d_v-1)(d_c-1)\big]^{i}. $$</p>
<p><b>Substitute.</b> $d_v=3$, $d_c=6$, so $d_v(d_c-1)=15$ and $(d_v-1)(d_c-1)=10$. Girth $6$: sum has one term ($i=0$). Girth $8$: two terms ($i=0,1$).</p>
<p><b>Compute.</b> Girth $\ge 6$: $n\ge 1+15\cdot1=16$. Girth $\ge 8$: $n\ge 1+15(1+10)=1+165=166$.</p>
<p><b>Explanation.</b> A $128$-bit $(3,6)$ code clears the girth-$6$ bar ($128\ge16$) but falls short of girth $8$ ($128<166$), so it <em>cannot</em> avoid length-$6$ cycles and must tolerate them. Sanity check: continuing the pattern gives girth $10\Rightarrow n\ge1666$ and girth $12\Rightarrow16666$ — each rung multiplies $n$ by about $10$, the exponential squeeze that keeps short codes at small girth.</p>` },
      { q: String.raw`A short code has $d_{\min}=10$ and multiplicity $A_{d_{\min}}=30$, rate $R=\tfrac12$. Estimate the union-bound BLER floor at $E_b/N_0=4\,\mathrm{dB}$.`, solution: String.raw`<p><b>Formula.</b> $$ \mathrm{BLER}\approx A_{d_{\min}}\,Q\!\left(\sqrt{2\,d_{\min}R\,\tfrac{E_b}{N_0}}\right). $$</p>
<p><b>Substitute.</b> $E_b/N_0=10^{4/10}=2.512$; argument $=\sqrt{2\cdot10\cdot0.5\cdot2.512}=\sqrt{25.12}$; $A_{d_{\min}}=30$.</p>
<p><b>Compute.</b> argument $=5.012$; $Q(5.012)=2.70\times10^{-7}$; $\mathrm{BLER}\approx 30\cdot2.70\times10^{-7}=8.1\times10^{-6}$.</p>
<p><b>Explanation.</b> The few weight-$10$ codewords pin the error rate near $10^{-5}$ even at a healthy SNR — a visible floor. Sanity check: at $5\,\mathrm{dB}$ the same formula gives $\approx2.8\times10^{-7}$ and at $3\,\mathrm{dB}$ about $1.2\times10^{-4}$, so the curve is falling but flattening onto the union-bound line — halving the multiplicity would halve the floor, and a larger $d_{\min}$ would steepen it.</p>` },
      { q: String.raw`For a $(128,64)$ code ($k=64$), count the OSD re-encodings at orders $1$, $2$ and $3$.`, solution: String.raw`<p><b>Formula.</b> $$ N_{\text{TEP}}(t)=\sum_{i=0}^{t}\binom{k}{i}. $$</p>
<p><b>Substitute.</b> $k=64$: $\binom{64}{0}=1$, $\binom{64}{1}=64$, $\binom{64}{2}=2016$, $\binom{64}{3}=41664$.</p>
<p><b>Compute.</b> Order $1$: $1+64=65$. Order $2$: $1+64+2016=2081$. Order $3$: $1+64+2016+41664=43745$.</p>
<p><b>Explanation.</b> Each extra order adds $\binom{k}{t}\sim k^t/t!$ patterns, so cost grows polynomially in $k$ — cheap because $k=64$ is small. Sanity check: order $2$ at $2081$ re-encodings is trivial on modern hardware and already gets a short code within a small fraction of a dB of maximum likelihood; the same order on a $k=8000$ long code would need $\binom{8000}{2}\approx3.2\times10^7$ patterns, which is why OSD is a short-code tool.</p>` },
      { q: String.raw`Rank the AWGN ($\mathrm{SNR}=1$), BSC ($p=0.11$) and BEC ($\epsilon_e=0.5$) channels, all at capacity $\approx 0.5$, by their short-block back-off, via the dispersion $V$.`, solution: String.raw`<p><b>Formula.</b> $$ V_{\text{AWGN}}=\tfrac{s(s+2)}{2(1+s)^2}(\log_2 e)^2,\quad V_{\text{BSC}}=p(1-p)\log_2^2\tfrac{1-p}{p},\quad V_{\text{BEC}}=\epsilon_e(1-\epsilon_e). $$</p>
<p><b>Substitute.</b> AWGN $s=1$: $\tfrac{3}{8}(1.4427)^2$. BSC $p=0.11$: $0.11\cdot0.89\cdot(\log_2(0.89/0.11))^2$. BEC $\epsilon_e=0.5$: $0.5\cdot0.5$.</p>
<p><b>Compute.</b> $V_{\text{AWGN}}=0.781$; $V_{\text{BSC}}=0.0979\cdot(3.017)^2=0.891$; $V_{\text{BEC}}=0.25$. Back-off $\propto\sqrt V$, so ordering (smallest $\to$ largest): BEC $<$ AWGN $<$ BSC.</p>
<p><b>Explanation.</b> Three channels of equal capacity impose unequal short-block penalties: the BEC is friendliest, the BSC harshest, with AWGN between. Sanity check: at $n=128$, $\epsilon=10^{-5}$ the back-off $\sqrt{V/n}\cdot4.265$ is $0.188$ (BEC), $0.333$ (AWGN), $0.356$ (BSC) bits — capacity alone could never have predicted this spread; only the dispersion does.</p>` },
      { q: String.raw`For $R=\tfrac12$, $\epsilon=10^{-5}$ on AWGN, the FBL bound requires SNR $=1.531$ at $n=500$ (vs SNR $=1$ at $n\to\infty$). Express the finite-length penalty of the bound itself as a gap to capacity in dB, and compare with $n=1000$ (SNR $=1.361$).`, solution: String.raw`<p><b>Formula.</b> $$ \Delta_{\mathrm{dB}}=10\log_{10}\frac{\mathrm{SNR}_{\text{FBL}}(n)}{\mathrm{SNR}_\infty},\qquad \mathrm{SNR}_\infty=2^{2R}-1. $$</p>
<p><b>Substitute.</b> $R=\tfrac12\Rightarrow\mathrm{SNR}_\infty=2^{1}-1=1$ ($0\,\mathrm{dB}$). $n=500$: $\mathrm{SNR}_{\text{FBL}}=1.531$. $n=1000$: $\mathrm{SNR}_{\text{FBL}}=1.361$.</p>
<p><b>Compute.</b> $n=500$: $\Delta_{\mathrm{dB}}=10\log_{10}(1.531/1)=1.85\,\mathrm{dB}$. $n=1000$: $\Delta_{\mathrm{dB}}=10\log_{10}1.361=1.34\,\mathrm{dB}$.</p>
<p><b>Explanation.</b> Even the <em>best possible</em> length-$500$ code needs $1.85\,\mathrm{dB}$ more than the capacity-limit SNR to hit rate $\tfrac12$ at $10^{-5}$; a real code adds its own gap on top. Sanity check: doubling $n$ from $500$ to $1000$ shrinks the bound's gap from $1.85$ to $1.34\,\mathrm{dB}$, consistent with the $\sqrt{V/n}$ back-off closing as $n$ grows — and this is why quoting a code's "gap to capacity" at short $n$ is misleading: most of it is this uncloseable FBL-bound gap.</p>` }
    ],
    realWorld: String.raw`<p>The clearest place to see this whole topic at work is <strong>5G URLLC</strong>, the ultra-reliable low-latency service designed for factory automation, motion control and vehicle-to-everything safety. Its requirement — deliver a payload of a few tens of bytes with a residual block error rate around $10^{-5}$ inside about a millisecond — is a single-packet problem by construction: the latency budget forbids long codewords, deep interleaving and retransmission, so one short codeword must succeed alone, and the metric is BLER, not BER. An engineer sizing this link cannot use the "$0.5\,\mathrm{dB}$ from capacity" reasoning of the data channel; at $n$ of a few hundred bits the finite-blocklength bound sits one to three decibels below capacity purely from the dispersion back-off $\sqrt{V/n}$, and that gap is physics, not something a better code removes. So the design proceeds exactly along this topic's lines: pick the operating rate and length, compare candidate codes against the PPV bound rather than against capacity, choose 5G LDPC <em>base graph 2</em> (the short-block, low-rate protograph) lifted quasi-cyclically to the required length, rely on PEG/ACE-style construction to keep the girth healthy and suppress the absorbing sets that would otherwise floor the code near the $10^{-5}$ target, and decode with layered min-sum belief propagation — reaching for near-ML post-processing where the last fraction of a dB matters. Every knob in that sentence is one this topic explains and the asymptotic LDPC theory does not.</p>
<p>The same short-packet thinking spreads well beyond one service. In <strong>NB-IoT and massive machine-type communication</strong>, billions of cheap battery-powered sensors send tiny payloads, and the binding constraints are energy and airtime, not throughput — so short codes with low decoding complexity, and an honest accounting of the finite-blocklength penalty, decide how far a coin-cell device can talk and how long it lasts. In <strong>WiFi</strong> the optional LDPC modes use short-to-moderate quasi-cyclic codes whose acknowledgements and control frames live squarely in the short regime, and in satellite command, telemetry and deep-space links the combination of latency and small packet sizes has pushed short protograph LDPC and near-ML decoding into active use. Across all of them the intellectual center of gravity has shifted from the asymptotic threshold — the pride of the general LDPC topic — to the quantities that govern one short shot: the dispersion and the FBL bound as the true benchmark, the minimum distance and its multiplicity setting an error floor, the trapping and absorbing sets that BP stumbles on, and the near-ML decoders (OSD, GRAND) that are affordable only because the code is small. And it is why 5G runs two capacity-approaching families side by side even at short lengths, polar on the tiniest control messages and LDPC on the data: the short single packet is demanding enough that each family is matched to the corner of the regime where it wins.</p>`,
    related: ['ldpc', 'tanner-graph', 'polar-codes', 'information-theory', 'turbo-codes']
  }
);
