// dummies-short-ldpc.js — plain-English "For Dummies" entry for Single-Packet Short LDPC Codes.
// Populates CONTENT_DUMMIES['short-ldpc'] (defined in content.js). No jargon.
Object.assign(CONTENT_DUMMIES, {
  'short-ldpc': String.raw`<p>Imagine you have to shout one short message across a noisy, crowded room to a friend — and here is the catch: you get exactly one shout, no repeats, and it has to be perfect. That is completely different from writing your friend a long letter. In a long letter, the odd smudged word does not matter; the reader fills in the gaps from all the surrounding text, and on average everything comes out fine. Most of the famous error-correcting codes are built like long letters: they work brilliantly because they are <b>long</b>, so the good and bad luck averages out. But a huge and growing amount of modern communication is the opposite — a single short shouted message you cannot repeat: a robot's emergency-stop signal, a car warning the car behind it, a tiny sensor sending one reading before going back to sleep, or the little setup message your phone must get right before it can connect. This topic is about protecting exactly those short, one-shot messages, and it turns out the rules of the game change completely when the message is short.</p>
<p>Why do the rules change? Because with a short message there is no room for luck to even out. Think of flipping a coin: flip it ten thousand times and you will get almost exactly half heads, dead reliable. Flip it just ten times and you might easily get seven heads or three — the result swings around wildly. A short message is like those ten flips: whether it arrives cleanly depends a lot on the particular bad luck of that one moment, not on some dependable long-run average. Engineers captured this with a beautiful piece of math (the "finite-blocklength" theory) that gives the honest speed limit for short messages. The punchline is humbling: the famous top speed everyone quotes for a channel is only reachable by very long messages. Cut the message short and you must automatically slow down by a penalty that gets worse the shorter you go — and no cleverness removes it, because it is a law of the situation, not a flaw in your code. So the fair question for a short code is never "how close to the theoretical top speed?" but "how close to the honest short-message limit?"</p>
<p>Once you accept that, a different set of worries takes over — and this is where these "LDPC" codes (a popular family that normally shines on long messages) need special care. In a short code there are only a handful of "weak spots": particular error patterns that are easy to confuse with a real message, so the code hits a stubborn floor it struggles to beat, like a stain that just will not wash out no matter how hard you scrub. There are also little tangles in the code's wiring — think of a few wires looped back on themselves — that confuse the usual quick decoder and make it get stuck; short codes cannot avoid these tangles because there simply is not enough wire to spread things out. The fixes are all about being clever with a small design: build the code from a tiny repeatable pattern stamped out many times (which also makes it easy to build in a chip), carefully arrange the wiring to dodge the worst tangles, and — because the message is short — you can even afford a slow, thorough "try lots of possibilities" decoder that would be hopeless on a long message. This is exactly why 5G phones use two different code families for short bursts: one kind for the tiniest control messages and this short-LDPC kind for short data. The whole art is squeezing every last drop of reliability out of a single short shout that you only get to make once.</p>`
});
