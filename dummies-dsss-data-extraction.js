// dummies-dsss-data-extraction.js — plain-English "For Dummies" entry for DSSS Data Extraction.
// Populates CONTENT_DUMMIES['dsss-data-extraction'] (defined in content.js). No jargon.
Object.assign(CONTENT_DUMMIES, {
  'dsss-data-extraction': String.raw`<p>Imagine a friend hides a short message inside a very long, very fast string of nonsense — a secret shuffle you both agreed on beforehand. To an eavesdropper it just sounds like static. But because <b>you know the exact shuffle</b>, you can "un-shuffle" what you hear: run the same nonsense pattern in reverse and the hidden message suddenly pops back out clear as day, while all the random junk that wasn't part of your secret gets smeared into a faint background hiss. That un-shuffling is <b>despreading</b>, and it is the first and most important move in pulling the data back out of a spread-spectrum signal.</p>
<p>After un-shuffling, the receiver does something like <b>tallying votes</b>. For each message bit it adds up a whole block of little yes/no nudges — hundreds or thousands of them. Any one nudge is drowned in noise and useless on its own, but add up enough of them and the honest signal piles up in one direction while the noise, being random, mostly cancels itself out. Whichever way the tally leans decides whether the bit was a 1 or a 0. Then a spell-checker for data (error-correction) fixes any leftover slips, and the real message is recovered.</p>
<p>Here's the twist people always get wrong. The clever shuffling does <b>not</b> make your signal magically punch through ordinary background noise better than a plain, simple radio would — at the same energy per bit, it makes exactly the same number of mistakes. What all that effort actually buys you is <b>protection against a bully</b>: if someone deliberately blasts a loud tone to jam you, the un-shuffling step smears the bully's power thin across the whole band, so only a tiny sliver of it lands on your message. That's why GPS, your phone, and military radios go to all this trouble — not for a cleaner day, but to survive a noisy, hostile one.</p>`
});
