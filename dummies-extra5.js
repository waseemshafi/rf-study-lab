/* ============================================================
   dummies-extra5.js — "For Dummies" intros for normal-distribution/
   error-function/rayleigh-distribution/awgn/trellis-diagram.
   Extends CONTENT_DUMMIES (defined in content.js).
   ============================================================ */
Object.assign(CONTENT_DUMMIES, {
  'normal-distribution': String.raw`<p>The <b>normal distribution</b> is the famous <b>bell curve</b> — the humped shape you get when most things cluster around an average and only a few land far out to either side. Measure the heights of a thousand people, the tiny errors in a thousand ruler readings, or the crackle of noise in a radio, and plot how often each value shows up: you get a tall bump in the middle that tails off smoothly on both sides. Most values are "pretty average," a decent number are a bit above or below, and genuinely extreme values are rare. That single, symmetric hump turns up so often that engineers treat it as the default picture of "random but well-behaved."</p><p>Why does nature keep drawing this exact shape? Because of a beautiful rule called the <b>central limit theorem</b>: whenever a result is the <b>sum of many small independent random nudges</b>, the total tends to pile up into a bell curve — no matter what the individual nudges looked like. A person's height is a mix of countless genetic and dietary factors; measurement error is the sum of many little slips; electrical noise is billions of jittering electrons added together. Stack up enough small randomness and the lumps average out into that same smooth hump, which is why the bell curve is everywhere.</p><p>The bell is pinned down by just two numbers: where its <b>center</b> (the mean, the average) sits, and how <b>wide</b> it spreads (the standard deviation). A handy rule of thumb, the <b>68-95-99.7 rule</b>, says about 68% of values land within one standard deviation of the center, about 95% within two, and about 99.7% within three. So values more than three "widths" from the middle are genuinely uncommon. This simple, predictable shape is the foundation for reasoning about noise, errors, and how confident you can be that a signal really carries the bit you think it does.</p>`,
  'error-function': String.raw`<p>The <b>error function</b> is a mathematical shortcut that answers one very practical question about the bell curve: <b>"what's the chance a random value lands way out in the tail?"</b> The area under a bell curve is what tells you the probability of an outcome, but that area has no tidy formula you can punch into a calculator by hand. So mathematicians pre-computed it once, packaged it up, and gave it a name. When you want the odds that noise pushes a value past some threshold, you look up the error function (or its close relatives) instead of grinding through the math every time — it's a ready-made table for "how much of the bell sticks out beyond here."</p><p>Engineers usually reach for two cousins of it: the <b>Q-function</b> and <b>erfc</b> ("erf-complement"). Both measure the same thing — the <b>tail area</b>, the sliver of the bell curve lying beyond a cutoff line. Picture the bell with a vertical line drawn some distance from the center; the little shaded slice out past that line is exactly the probability these functions hand you. The farther out the line, the thinner the slice, and the smaller the chance of landing there.</p><p>This is the everyday tool that turns <b>signal-to-noise ratio</b> into a <b>bit-error probability</b>. A receiver decides "was that a 1 or a 0?" by checking which side of a threshold the noisy signal fell on. An error happens only when noise is strong enough to shove the value across that line — which is precisely a tail-area question. Feed the signal-to-noise ratio into the Q-function or erfc and out pops the expected error rate. That's why these functions sit at the heart of every link-budget calculation and every "how good is this modem?" comparison — they convert a strength ratio into a real-world chance of getting a bit wrong.</p>`,
  'rayleigh-distribution': String.raw`<p>The <b>Rayleigh distribution</b> describes how a radio signal's strength wanders up and down when it <b>bounces off buildings, walls, and cars</b> on its way to your phone. In a city there is rarely one clean path from tower to handset; instead the same signal arrives as <b>many echoes</b>, each taking a slightly different route and arriving a hair out of step with the others. Sometimes those echoes line up and reinforce each other (strong signal), and sometimes they cancel out (weak signal). This tug-of-war between random echoes is called <b>multipath fading</b>, and the Rayleigh curve is the shape that describes how likely each strength level is.</p><p>Add up a big pile of randomly-timed echoes and the combined strength turns out to have a distinctive <b>lopsided</b> shape: <b>mostly medium, occasionally strong, and every so often terrifyingly close to zero</b>. Those near-zero moments are the dangerous ones — called <b>deep fades</b> — where the echoes momentarily cancel almost perfectly and the signal briefly collapses. Move the phone a few centimeters, or wait a fraction of a second, and the pattern shifts. Deep fades are exactly what make a call crackle, freeze, or drop even when you seem to have decent coverage.</p><p>Because these dropouts are unavoidable in a cluttered environment, engineers plan around them. They build in a <b>fade margin</b> — deliberately transmitting with extra strength to spare, like packing more money than you expect to spend, so that even during a fade there's still enough signal to get through. They also use <b>diversity</b>: multiple antennas or multiple frequencies so that when one path is in a deep fade, another probably isn't. Understanding the Rayleigh shape is what lets designers predict how often fades will strike and how much margin and diversity it takes to keep your calls alive.</p>`,
  'awgn': String.raw`<p><b>AWGN</b> stands for <b>Additive White Gaussian Noise</b>, and it's the <b>standard background hiss</b> that every radio receiver has to fight against. No matter how clean your transmitter is, the receiver's own electronics and the warmth of the universe itself generate a faint, random crackle — the same "shhh" you hear on an untuned FM radio. It's the ever-present floor of randomness that a real signal has to rise above in order to be understood. Every real-world link, from your Wi-Fi to a deep-space probe, is competing against some amount of this hiss.</p><p>Each word in the name pins down one property. <b>Additive</b> means the noise simply <b>gets added on top</b> of your signal — it doesn't distort or stretch what you sent, it just lays a layer of fuzz over it, like static overlaid on a photo. <b>White</b> means the noise is <b>equally strong at every frequency</b>, just as white light contains every color mixed together — there's no particular pitch to it, it hisses evenly across the whole band. <b>Gaussian</b> means the moment-to-moment randomness follows the <b>bell curve</b>: small wiggles are common, big jumps are rare, with that same predictable hump shape from the central limit theorem.</p><p>Because it is so simple and so universal, AWGN is <b>the textbook yardstick</b> every radio design is measured against. When engineers want to compare two modems, error-correcting codes, or antenna schemes fairly, they test each one over an idealized AWGN channel — the cleanest possible "just add hiss" environment. If a system can't handle plain AWGN, it has no hope against the messier real world of fading and interference. That's why nearly every performance curve, every "bit-error rate versus signal-to-noise" chart, and every benchmark starts from this single, standardized notion of noise.</p>`,
  'trellis-diagram': String.raw`<p>A <b>trellis diagram</b> is a way of drawing out a repeating "<b>what state am I in right now?</b>" machine so you can see it evolving over time. Many encoders have a small memory — they remember the last few bits — so at any moment the encoder is in one of a handful of possible <b>states</b>, and each new input bit both produces an output and flips it into the next state. Instead of picturing this as one machine looping on itself, a trellis <b>unrolls it into a lattice</b>: one column of dots for every tick of the clock, with lines connecting each state to the states it could move to next. Read left to right, it lays the machine's entire life story out flat.</p><p>The magic is that this layout turns decoding into a <b>route-finding puzzle</b>. Think of it like a <b>subway map with a column of stations for each moment in time</b>: every possible sequence the encoder could have sent is just a path threading left-to-right through the lattice, hopping from one state to the next. The receiver's noisy measurements assign each little hop a "cost" — how well it matches what was actually heard. Finding what was really transmitted becomes finding the <b>single cheapest path</b> across the whole map, the route that disagrees least with the received signal.</p><p>This is exactly the backbone of <b>Viterbi decoding</b>, the workhorse behind convolutional codes in Wi-Fi, cellular, GPS, and satellite links. Rather than checking every possible path one by one (there are astronomically many), the Viterbi algorithm sweeps through the trellis column by column, at each state keeping only the best route that could have arrived there and throwing the rest away. By the final column it has efficiently found the most-likely path through the whole lattice — and that winning path spells out the decoded message.</p>`
});
